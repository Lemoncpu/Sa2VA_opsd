# Sa2VA / tools 源码说明文档

本文档覆盖 `projects/sa2va/` 与 `tools/` 下的 `.py` / `.sh` 代码文件。

- 统计口径：仅源码文件，忽略 `README`、缓存文件和二进制产物。
- 文件总数：149。
- 行号说明：行号为源码中定义起始行，便于快速跳转。
- 说明粒度：对每个文件给出职责概述，并列出类、方法、顶层函数或 shell 函数的具体用途。

## `projects/sa2va/configs/refcoco_caption_to_mask_eval_2b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：22
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/refcoco_mask_closure_eval_2b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：42
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/refcoco_mask_closure_eval_4b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：53
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/refcoco_teacher_context_validation_2b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：44
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/refcoco_teacher_context_validation_4b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：41
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_finetune.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：280
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_in30_14b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：418
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_in30_2b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：418
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_in30_8b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：418
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_internvl3_2b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：128
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_internvl3_2b_v2.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：140
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_internvl3_2b_v2_balanced.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：145
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_internvl3_2b_v2_target.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：146
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_internvl3_2b_v3.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：153
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_internvl3_2b_v3_balanced.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：151
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_internvl3_2b_v3_target.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：152
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_refcoco_internvl3_2b_v3.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：214
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_refcoco_internvl3_2b_v3_online.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：25
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_refcoco_internvl3_4b_v3.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：220
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_refcoco_internvl3_4b_v3_online.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：25
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_opsd_refval100_v2_grpo_smoke.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：139
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_qwen_finetune.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：264
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_qwenvl25/sa2va_qwen25_3b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：433
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_qwenvl25/sa2va_qwen25_7b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：433
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/configs/sa2va_qwenvl3/sa2va_qwen3_4b.py`

- 文件职责：配置文件：定义模型、数据集、优化器、hook、运行参数或导出参数；通常不包含函数。
- 行数：433
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/datasets/__init__.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：64
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/datasets/base.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：540
- 类 `Sa2VADatasetMixin`（行 17）：类定义，承载相关状态和行为。
  - 方法 `_init_architecture_config`（行 31）：初始化内部子模块、配置或运行时状态。
  - 方法 `_init_image_processing_config`（行 50）：初始化内部子模块、配置或运行时状态。
  - 方法 `_init_tokenizer`（行 81）：初始化内部子模块、配置或运行时状态。
  - 方法 `_init_image_processor`（行 87）：初始化内部子模块、配置或运行时状态。
  - 方法 `_init_extra_image_processor`（行 101）：初始化内部子模块、配置或运行时状态。
  - 方法 `_setup_system_prompt`（行 108）：Sa2VADatasetMixin 的内部辅助方法。
  - 方法 `_process_single_image`（行 112）：Sa2VADatasetMixin 的内部辅助方法。
  - 方法 `_process_multiple_images`（行 170）：Sa2VADatasetMixin 的内部辅助方法。
  - 方法 `_create_token_string`（行 232）：创建新的提示、张量、字符串或数据记录。
  - 方法 `_create_image_token_string`（行 260）：创建新的提示、张量、字符串或数据记录。
  - 方法 `_process_conversations_for_encoding`（行 264）：Sa2VADatasetMixin 的内部辅助方法。
  - 方法 `get_inputid_labels`（行 324）：获取派生值、状态、路径或对象。
  - 方法 `_expand_video_tokens`（行 343）：Sa2VADatasetMixin 的内部辅助方法。
  - 方法 `_get_modality_length_default`（行 364）：Sa2VADatasetMixin 的内部辅助方法。
  - 方法 `_read_image`（行 368）：Sa2VADatasetMixin 的内部辅助方法。
  - 方法 `_check_image_exists`（行 386）：Sa2VADatasetMixin 的内部辅助方法。
- 类 `Sa2VABaseDataset`（行 405）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 411）：初始化对象状态、依赖和运行参数。
  - 方法 `__len__`（行 478）：返回数据集或容器长度。
  - 方法 `real_len`（行 482）：返回未做重复采样前的真实长度。
  - 方法 `_get_index_mapping`（行 486）：Sa2VABaseDataset 的内部辅助方法。
  - 方法 `shuffle_indices`（行 505）：Sa2VABaseDataset 的成员方法。
  - 方法 `__getitem__`（行 512）：按索引读取并构造单条样本。
  - 方法 `_rand_another_index`（行 529）：Sa2VABaseDataset 的内部辅助方法。
  - 方法 `prepare_data`（行 533）：把原始样本整理成模型可消费的输入结构。
  - 方法 `modality_length`（行 538）：返回模态长度，供采样器或分桶逻辑使用。

## `projects/sa2va/datasets/common.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：79
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/datasets/data_utils.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：412
- 类 `ConcatDatasetSa2VA`（行 374）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 376）：初始化对象状态、依赖和运行参数。
  - 方法 `__repr__`（行 390）：返回对象的可读字符串表示。
- 函数 `find_closest_aspect_ratio`（行 16）：搜索最匹配的对象、索引或路径。
- 函数 `dynamic_preprocess`（行 32）：提供该文件的一段辅助逻辑。
- 函数 `tokenize_conversation`（行 74）：把文本或对话编码成 token 序列。
- 函数 `template_map_fn`（行 157）：把数据映射为模板化输入格式。
- 函数 `sa2va_collect_fn`（行 184）：提供该文件的一段辅助逻辑。
- 函数 `sam2_path_patch`（行 310）：提供该文件的一段辅助逻辑。
- 函数 `get_video_frames`（行 337）：获取派生值、状态、路径或对象。
- 函数 `decode_masklet`（行 360）：把编码后的对象还原为可操作的数据结构。
- 函数 `opencvimg_to_pil`（行 368）：提供该文件的一段辅助逻辑。
- 函数 `sa2va_opsd_collect_fn`（行 396）：提供该文件的一段辅助逻辑。

## `projects/sa2va/datasets/data_utils_opsd_v2.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：31
- 函数 `sa2va_opsd_collect_fn_v2`（行 4）：提供该文件的一段辅助逻辑。

## `projects/sa2va/datasets/refcoco_opsd.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：543
- 类 `Sa2VAOpsdRefCocoDataset`（行 125）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 126）：初始化对象状态、依赖和运行参数。
  - 方法 `modality_length`（行 205）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `resolve_active_route_manifest_path`（行 208）：Sa2VAOpsdRefCocoDataset 的成员方法。
  - 方法 `load_route_manifest_file`（行 212）：从文件或配置中加载数据。
  - 方法 `load_route_manifest`（行 231）：从文件或配置中加载数据。
  - 方法 `_apply_route_manifest_filter`（行 248）：Sa2VAOpsdRefCocoDataset 的内部辅助方法。
  - 方法 `refresh_route_manifest_if_needed`（行 263）：Sa2VAOpsdRefCocoDataset 的成员方法。
  - 方法 `get_route_manifest_version`（行 277）：获取派生值、状态、路径或对象。
  - 方法 `get_route_for_sample_key`（行 280）：获取派生值、状态、路径或对象。
  - 方法 `get_sample_key_for_index`（行 284）：获取派生值、状态、路径或对象。
  - 方法 `get_route_info_for_index`（行 288）：获取派生值、状态、路径或对象。
  - 方法 `get_route_for_index`（行 292）：获取派生值、状态、路径或对象。
  - 方法 `get_route_distribution`（行 295）：获取派生值、状态、路径或对象。
  - 方法 `__len__`（行 303）：返回数据集或容器长度。
  - 方法 `_to_prompt_masks`（行 307）：Sa2VAOpsdRefCocoDataset 的内部辅助方法。
  - 方法 `_decode_coco_rle`（行 311）：Sa2VAOpsdRefCocoDataset 的内部辅助方法。
  - 方法 `_mask_area_ratio`（行 322）：围绕 mask 的几何属性、可视化或统计提供辅助逻辑。
  - 方法 `_mask_center`（行 329）：围绕 mask 的几何属性、可视化或统计提供辅助逻辑。
  - 方法 `_mask_center_distance`（行 338）：围绕 mask 的几何属性、可视化或统计提供辅助逻辑。
  - 方法 `_compute_iou`（行 346）：计算内部指标、几何关系或辅助数值。
  - 方法 `_prepare_mask_like_gt`（行 356）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_score_confuser_candidate`（行 367）：对候选项、样本或轨迹进行打分。
  - 方法 `_candidate_pool_dataset_names`（行 375）：Sa2VAOpsdRefCocoDataset 的内部辅助方法。
  - 方法 `_resolve_sam_confuser_pool_path`（行 386）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_load_sam_confuser_pool`（行 397）：加载内部所需的资源、配置或标注数据。
  - 方法 `_select_sam_pool_confusers`（行 413）：从候选集合中筛选符合条件的项。
  - 方法 `prepare_data`（行 481）：把原始样本整理成模型可消费的输入结构。
  - 方法 `_refetch_index`（行 530）：Sa2VAOpsdRefCocoDataset 的内部辅助方法。
  - 方法 `__getitem__`（行 535）：按索引读取并构造单条样本。
- 函数 `_resolve_dataset_meta`（行 33）：解析并确定运行时所需的路径、配置、状态或目标对象。
- 函数 `decode_refcoco_mask`（行 39）：把编码后的对象还原为可操作的数据结构。
- 函数 `resolve_refcoco_image_root`（行 55）：提供该文件的一段辅助逻辑。
- 函数 `build_refcoco_opsd_records`（行 77）：构建对外可用的数据结构、模型、提示词或运行对象。

## `projects/sa2va/datasets/sa2va_data_01_refseg.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：209
- 类 `Sa2VA01RefSeg`（行 13）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 15）：初始化对象状态、依赖和运行参数。
  - 方法 `modality_length`（行 63）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `_parse_annotations`（行 66）：Sa2VA01RefSeg 的内部辅助方法。
  - 方法 `prepare_data`（行 106）：把原始样本整理成模型可消费的输入结构。
  - 方法 `mock_prepare_data`（行 138）：Sa2VA01RefSeg 的成员方法。
  - 方法 `real_len`（行 177）：返回未做重复采样前的真实长度。
  - 方法 `__len__`（行 190）：返回数据集或容器长度。
  - 方法 `__getitem__`（行 194）：按索引读取并构造单条样本。

## `projects/sa2va/datasets/sa2va_data_02_vqa.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：124
- 类 `LLaVADataset`（行 9）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 11）：初始化对象状态、依赖和运行参数。
  - 方法 `_load_annotations`（行 39）：加载内部所需的资源、配置或标注数据。
  - 方法 `prepare_data`（行 43）：把原始样本整理成模型可消费的输入结构。
  - 方法 `real_len`（行 78）：返回未做重复采样前的真实长度。
  - 方法 `modality_length`（行 83）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `mock_prepare_data`（行 86）：LLaVADataset 的成员方法。

## `projects/sa2va/datasets/sa2va_data_03_refvos.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：459
- 类 `Sa2VA03RefVOS`（行 16）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 18）：初始化对象状态、依赖和运行参数。
  - 方法 `real_len`（行 71）：返回未做重复采样前的真实长度。
  - 方法 `json_file_preprocess`（行 74）：Sa2VA03RefVOS 的成员方法。
  - 方法 `decode_mask`（行 126）：把序列化 mask 还原为二维数组。
  - 方法 `prepare_data`（行 162）：把原始样本整理成模型可消费的输入结构。
  - 方法 `dataset_map_fn_refsav`（行 276）：Sa2VA03RefVOS 的成员方法。
  - 方法 `dataset_map_fn`（行 309）：把原始数据映射成训练样本结构。
  - 方法 `modality_length`（行 381）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `mock_prepare_data`（行 387）：Sa2VA03RefVOS 的成员方法。

## `projects/sa2va/datasets/sa2va_data_04_videoqa.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：205
- 类 `Sa2VA04VideoQA`（行 40）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 48）：初始化对象状态、依赖和运行参数。
  - 方法 `real_len`（行 83）：返回未做重复采样前的真实长度。
  - 方法 `json_file_preprocess`（行 86）：Sa2VA04VideoQA 的成员方法。
  - 方法 `prepare_data`（行 92）：把原始样本整理成模型可消费的输入结构。
  - 方法 `dataset_map_fn`（行 135）：把原始数据映射成训练样本结构。
  - 方法 `mock_prepare_data`（行 168）：Sa2VA04VideoQA 的成员方法。
  - 方法 `modality_length`（行 204）：返回模态长度，供采样器或分桶逻辑使用。
- 函数 `_get_rawvideo_dec`（行 13）：提供该文件的一段辅助逻辑。

## `projects/sa2va/datasets/sa2va_data_05_gcg.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：400
- 类 `Sa2VA05GCGDataset`（行 15）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 25）：初始化对象状态、依赖和运行参数。
  - 方法 `_load_annotations`（行 72）：加载内部所需的资源、配置或标注数据。
  - 方法 `_load_generic_annotations`（行 82）：加载内部所需的资源、配置或标注数据。
  - 方法 `_load_refcocog_annotations`（行 88）：加载内部所需的资源、配置或标注数据。
  - 方法 `_load_flickr30k_annotations`（行 95）：加载内部所需的资源、配置或标注数据。
  - 方法 `real_len`（行 124）：返回未做重复采样前的真实长度。
  - 方法 `modality_length`（行 129）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `_parse_generic_annotations`（行 132）：Sa2VA05GCGDataset 的内部辅助方法。
  - 方法 `_parse_refcocog_annotations`（行 170）：Sa2VA05GCGDataset 的内部辅助方法。
  - 方法 `_parse_flickr30k_annotations`（行 213）：Sa2VA05GCGDataset 的内部辅助方法。
  - 方法 `_create_gcg_conversation`（行 264）：创建新的提示、张量、字符串或数据记录。
  - 方法 `prepare_data`（行 283）：把原始样本整理成模型可消费的输入结构。
  - 方法 `mock_prepare_data`（行 329）：Sa2VA05GCGDataset 的成员方法。
- 类 `Sa2VA05RefCOCOgGCGDataset`（行 374）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 376）：初始化对象状态、依赖和运行参数。
- 类 `Sa2VA05GranDfGCGDataset`（行 381）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 383）：初始化对象状态、依赖和运行参数。
- 类 `Sa2VA05FlickrGCGDataset`（行 388）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 390）：初始化对象状态、依赖和运行参数。
- 类 `Sa2VA05OpenPsgGCGDataset`（行 395）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 397）：初始化对象状态、依赖和运行参数。

## `projects/sa2va/datasets/sa2va_data_06_vp.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：417
- 类 `Sa2VA06VPDataset`（行 14）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 26）：初始化对象状态、依赖和运行参数。
  - 方法 `_load_annotations`（行 81）：加载内部所需的资源、配置或标注数据。
  - 方法 `real_len`（行 87）：返回未做重复采样前的真实长度。
  - 方法 `modality_length`（行 92）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `_decode_masks`（行 95）：Sa2VA06VPDataset 的内部辅助方法。
  - 方法 `_get_region_infos`（行 122）：Sa2VA06VPDataset 的内部辅助方法。
  - 方法 `_create_region_prompt`（行 156）：创建新的提示、张量、字符串或数据记录。
  - 方法 `_parse_conversation_format`（行 168）：Sa2VA06VPDataset 的内部辅助方法。
  - 方法 `_parse_description_format`（行 201）：Sa2VA06VPDataset 的内部辅助方法。
  - 方法 `_find_image_path`（行 235）：Sa2VA06VPDataset 的内部辅助方法。
  - 方法 `_process_conversations`（行 247）：Sa2VA06VPDataset 的内部辅助方法。
  - 方法 `_create_description_conversations`（行 282）：创建新的提示、张量、字符串或数据记录。
  - 方法 `prepare_data`（行 301）：把原始样本整理成模型可消费的输入结构。
  - 方法 `mock_prepare_data`（行 344）：Sa2VA06VPDataset 的成员方法。
- 类 `Sa2VA06OspreyDataset`（行 385）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 387）：初始化对象状态、依赖和运行参数。
- 类 `Sa2VA06OspreyDescriptionDataset`（行 392）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 394）：初始化对象状态、依赖和运行参数。
- 类 `Sa2VA06OspreyShortDescriptionDataset`（行 399）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 401）：初始化对象状态、依赖和运行参数。
- 类 `Sa2VA06OspreyPartDataset`（行 406）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 408）：初始化对象状态、依赖和运行参数。
- 类 `Sa2VA06OspreyPositiveNegDataset`（行 413）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 415）：初始化对象状态、依赖和运行参数。

## `projects/sa2va/datasets/sa2va_data_finetune.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：179
- 类 `Sa2VAFinetuneDataset`（行 15）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 17）：初始化对象状态、依赖和运行参数。
  - 方法 `load_data_list`（行 61）：从文件或配置中加载数据。
  - 方法 `modality_length`（行 82）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `_parse_annotations`（行 85）：Sa2VAFinetuneDataset 的内部辅助方法。
  - 方法 `prepare_data`（行 125）：把原始样本整理成模型可消费的输入结构。
  - 方法 `real_len`（行 157）：返回未做重复采样前的真实长度。
  - 方法 `__len__`（行 160）：返回数据集或容器长度。
  - 方法 `__getitem__`（行 164）：按索引读取并构造单条样本。

## `projects/sa2va/datasets/sa2va_opsd_npz.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：132
- 类 `Sa2VAOpsdNPZDataset`（行 17）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 20）：初始化对象状态、依赖和运行参数。
  - 方法 `modality_length`（行 48）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `__len__`（行 51）：返回数据集或容器长度。
  - 方法 `_load_raw_npz`（行 55）：加载内部所需的资源、配置或标注数据。
  - 方法 `_normalize_frame`（行 63）：Sa2VAOpsdNPZDataset 的内部辅助方法。
  - 方法 `_normalize_mask`（行 85）：Sa2VAOpsdNPZDataset 的内部辅助方法。
  - 方法 `prepare_data`（行 95）：把原始样本整理成模型可消费的输入结构。
  - 方法 `_rand_another`（行 123）：Sa2VAOpsdNPZDataset 的内部辅助方法。
  - 方法 `__getitem__`（行 126）：按索引读取并构造单条样本。

## `projects/sa2va/datasets/sa2va_opsd_npz_v2.py`

- 文件职责：数据集与数据整理模块：负责读取标注、采样样本、构造多模态训练输入。
- 行数：143
- 类 `Sa2VAOpsdNPZDatasetV2`（行 17）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 20）：初始化对象状态、依赖和运行参数。
  - 方法 `modality_length`（行 51）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `__len__`（行 54）：返回数据集或容器长度。
  - 方法 `_load_raw_npz`（行 58）：加载内部所需的资源、配置或标注数据。
  - 方法 `_normalize_frame`（行 67）：Sa2VAOpsdNPZDatasetV2 的内部辅助方法。
  - 方法 `_normalize_mask`（行 89）：Sa2VAOpsdNPZDatasetV2 的内部辅助方法。
  - 方法 `_to_prompt_masks`（行 100）：Sa2VAOpsdNPZDatasetV2 的内部辅助方法。
  - 方法 `prepare_data`（行 103）：把原始样本整理成模型可消费的输入结构。
  - 方法 `_refetch_index`（行 132）：Sa2VAOpsdNPZDatasetV2 的内部辅助方法。
  - 方法 `__getitem__`（行 137）：按索引读取并构造单条样本。

## `projects/sa2va/evaluation/caption_to_mask_common.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：218
- 函数 `normalize_refcoco_caption`（行 11）：规范化输入数据、路径或文本表示。
- 函数 `run_caption_to_mask_eval`（行 16）：提供该文件的一段辅助逻辑。
- 函数 `_load_eval_image`（行 110）：加载内部所需的资源、配置或标注数据。
- 函数 `run_mask_to_caption_to_mask_eval`（行 121）：提供该文件的一段辅助逻辑。

## `projects/sa2va/evaluation/dataset/RES.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：292
- 类 `RESDataset`（行 15）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `metainfo_name`（行 19）：RESDataset 的成员方法。
  - 方法 `__init__`（行 22）：初始化对象状态、依赖和运行参数。
  - 方法 `_set_attribute`（行 36）：RESDataset 的内部辅助方法。
  - 方法 `__len__`（行 41）：返回数据集或容器长度。
  - 方法 `real_len`（行 44）：返回未做重复采样前的真实长度。
  - 方法 `json_file_preprocess`（行 47）：RESDataset 的成员方法。
  - 方法 `create_img_to_refs_mapping`（行 96）：RESDataset 的成员方法。
  - 方法 `decode_mask`（行 102）：把序列化 mask 还原为二维数组。
  - 方法 `only_get_text_infos`（行 180）：RESDataset 的成员方法。
  - 方法 `get_questions`（行 183）：获取派生值、状态、路径或对象。
  - 方法 `filter_data_dict`（行 198）：依据条件过滤数据或记录。
  - 方法 `__getitem__`（行 203）：按索引读取并构造单条样本。
  - 方法 `evaluate`（行 226）：根据预测结果计算评估指标。
- 函数 `rle_to_mask`（行 285）：提供该文件的一段辅助逻辑。

## `projects/sa2va/evaluation/dataset/__init__.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：2
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/evaluation/dataset/base_eval_dataset.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：69
- 类 `BaseEvalDataset`（行 12）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 16）：初始化对象状态、依赖和运行参数。
  - 方法 `_load_metainfo`（行 20）：加载内部所需的资源、配置或标注数据。
  - 方法 `metainfo`（行 58）：BaseEvalDataset 的成员方法。
  - 方法 `evaluate`（行 68）：根据预测结果计算评估指标。

## `projects/sa2va/evaluation/dataset/opsd_refcoco_style.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：189
- 类 `OPSDRefcocoStyleDataset`（行 13）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 16）：初始化对象状态、依赖和运行参数。
  - 方法 `metainfo_name`（行 25）：OPSDRefcocoStyleDataset 的成员方法。
  - 方法 `_load_items`（行 28）：加载内部所需的资源、配置或标注数据。
  - 方法 `__len__`（行 44）：返回数据集或容器长度。
  - 方法 `real_len`（行 47）：返回未做重复采样前的真实长度。
  - 方法 `_resolve_image_path`（行 50）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_load_npz_sample`（行 57）：加载内部所需的资源、配置或标注数据。
  - 方法 `decode_mask`（行 63）：把序列化 mask 还原为二维数组。
  - 方法 `only_get_text_infos`（行 72）：OPSDRefcocoStyleDataset 的成员方法。
  - 方法 `get_questions`（行 75）：获取派生值、状态、路径或对象。
  - 方法 `filter_data_dict`（行 96）：依据条件过滤数据或记录。
  - 方法 `__getitem__`（行 100）：按索引读取并构造单条样本。
  - 方法 `evaluate`（行 121）：根据预测结果计算评估指标。
- 函数 `rle_to_mask`（行 183）：提供该文件的一段辅助逻辑。

## `projects/sa2va/evaluation/dataset/refVOS.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：106
- 类 `RefVOSDataset`（行 17）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 18）：初始化对象状态、依赖和运行参数。
  - 方法 `__len__`（行 32）：返回数据集或容器长度。
  - 方法 `real_len`（行 35）：返回未做重复采样前的真实长度。
  - 方法 `json_file_preprocess`（行 38）：RefVOSDataset 的成员方法。
  - 方法 `__getitem__`（行 69）：按索引读取并构造单条样本。

## `projects/sa2va/evaluation/dataset/ref_sav.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：458
- 类 `VideoRefSAM2EvalDataset`（行 76）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 89）：初始化对象状态、依赖和运行参数。
  - 方法 `__len__`（行 176）：返回数据集或容器长度。
  - 方法 `modality_length`（行 180）：返回模态长度，供采样器或分桶逻辑使用。
  - 方法 `real_len`（行 187）：返回未做重复采样前的真实长度。
  - 方法 `json_file_preprocess`（行 190）：VideoRefSAM2EvalDataset 的成员方法。
  - 方法 `create_img_to_refs_mapping`（行 233）：VideoRefSAM2EvalDataset 的成员方法。
  - 方法 `dataset_map_fn`（行 239）：把原始数据映射成训练样本结构。
  - 方法 `prepare_text`（行 262）：VideoRefSAM2EvalDataset 的成员方法。
  - 方法 `__getitem__`（行 313）：按索引读取并构造单条样本。
  - 方法 `evaluate`（行 390）：根据预测结果计算评估指标。
- 函数 `get_video_frames`（行 36）：获取派生值、状态、路径或对象。
- 函数 `multi_template_fn`（行 52）：提供该文件的一段辅助逻辑。

## `projects/sa2va/evaluation/dist_test.sh`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：33
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `projects/sa2va/evaluation/metrics_gcg.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：306
- 函数 `parse_args`（行 14）：解析命令行参数。
- 函数 `get_bert_embedding`（行 33）：获取派生值、状态、路径或对象。
- 函数 `compute_iou`（行 41）：计算两个 mask 的 IoU。
- 函数 `bbox_to_x1y1x2y2`（行 48）：提供该文件的一段辅助逻辑。
- 函数 `compute_miou`（行 54）：提供该文件的一段辅助逻辑。
- 函数 `evaluate_mask_miou`（行 72）：提供该文件的一段辅助逻辑。
- 函数 `compute_iou_matrix`（行 100）：提供该文件的一段辅助逻辑。
- 函数 `text_similarity_bert`（行 109）：提供该文件的一段辅助逻辑。
- 函数 `find_best_matches`（行 116）：搜索最匹配的对象、索引或路径。
- 函数 `evaluate_recall_with_mapping`（行 147）：提供该文件的一段辅助逻辑。
- 函数 `main`（行 198）：脚本主入口，组织完整执行流程。

## `projects/sa2va/evaluation/refcoco_eval_common.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：157
- 函数 `mask_to_rle`（行 14）：把二值 mask 编码为 RLE。
- 函数 `build_model_components`（行 23）：构建模型与相关组件。
- 函数 `build_dataloader`（行 44）：构建 dataloader。
- 函数 `run_refcoco_style_eval`（行 62）：执行 refcoco 风格评测。

## `projects/sa2va/evaluation/run_all_evals.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：81
- 函数 `run_command`（行 5）：执行外部命令并处理返回码。
- 函数 `main`（行 16）：脚本主入口，组织完整执行流程。

## `projects/sa2va/evaluation/sa2va_eval_gcg.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：264
- 类 `GCGInferenceDataset`（行 45）：数据集类，封装样本读取、转换和索引逻辑。
  - 方法 `__init__`（行 46）：初始化对象状态、依赖和运行参数。
  - 方法 `__len__`（行 65）：返回数据集或容器长度。
  - 方法 `get_questions`（行 68）：获取派生值、状态、路径或对象。
  - 方法 `__getitem__`（行 73）：按索引读取并构造单条样本。
- 函数 `parse_args`（行 19）：解析命令行参数。
- 函数 `main`（行 88）：脚本主入口，组织完整执行流程。
- 函数 `process_and_save_output`（行 183）：提供该文件的一段辅助逻辑。
- 函数 `mask_to_rle_pytorch`（行 228）：提供该文件的一段辅助逻辑。
- 函数 `coco_encode_rle`（行 256）：提供该文件的一段辅助逻辑。

## `projects/sa2va/evaluation/sa2va_eval_opsd_refcoco_style.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：83
- 函数 `parse_args`（行 14）：解析命令行参数。
- 函数 `main`（行 27）：脚本主入口，组织完整执行流程。

## `projects/sa2va/evaluation/sa2va_eval_ref_vos.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：241
- 函数 `async_func`（行 22）：提供该文件的一段辅助逻辑。
- 函数 `mask_to_rle`（行 27）：把二值 mask 编码为 RLE。
- 函数 `mask_save`（行 35）：提供该文件的一段辅助逻辑。
- 函数 `parse_args`（行 93）：解析命令行参数。

## `projects/sa2va/evaluation/sa2va_eval_refcoco.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：78
- 函数 `parse_args`（行 15）：解析命令行参数。
- 函数 `main`（行 58）：脚本主入口，组织完整执行流程。

## `projects/sa2va/evaluation/teacher_diagnosis_common.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：682
- 函数 `normalize_teacher_output`（行 16）：规范化输入数据、路径或文本表示。
- 函数 `parse_labeled_sections`（行 22）：提供该文件的一段辅助逻辑。
- 函数 `labeled_field_count`（行 33）：提供该文件的一段辅助逻辑。
- 函数 `is_degenerate_teacher_output`（行 37）：提供该文件的一段辅助逻辑。
- 函数 `score_labeled_output`（行 71）：提供该文件的一段辅助逻辑。
- 函数 `format_teacher_structured_output`（行 82）：提供该文件的一段辅助逻辑。
- 函数 `build_mask_relation_context`（行 94）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `build_region_description_fallback`（行 109）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `_is_region_description_usable`（行 119）：提供该文件的一段辅助逻辑。
- 函数 `describe_region_with_retries`（行 130）：提供该文件的一段辅助逻辑。
- 函数 `caption_to_mask_seg_correct`（行 157）：提供该文件的一段辅助逻辑。
- 函数 `classify_teacher_route`（行 161）：提供该文件的一段辅助逻辑。
- 函数 `_bool_text`（行 175）：提供该文件的一段辅助逻辑。
- 函数 `build_compare_prompt_variants`（行 179）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `build_compare_fallback`（行 270）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `_empty_teacher_metrics`（行 325）：创建零值/空值占位结果。
- 函数 `_build_caption_reconstruction_metrics`（行 345）：构造中间对象、提示词、样本结构或运行配置。
- 函数 `_build_teacher_predict_reconstruction_metrics`（行 399）：构造中间对象、提示词、样本结构或运行配置。
- 函数 `_build_teacher_regenerate_reconstruction_metrics`（行 420）：构造中间对象、提示词、样本结构或运行配置。
- 函数 `_build_teacher_diagnosis_fields_staged`（行 455）：构造中间对象、提示词、样本结构或运行配置。
- 函数 `build_teacher_diagnosis_fields_staged`（行 641）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `build_teacher_context_validation_fields`（行 663）：构建对外可用的数据结构、模型、提示词或运行对象。

## `projects/sa2va/evaluation/utils/__init__.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：4
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/evaluation/utils/dist.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：282
- 函数 `_init_dist_pytorch`（行 14）：初始化内部子模块、配置或运行时状态。
- 函数 `_init_dist_slurm`（行 29）：初始化内部子模块、配置或运行时状态。
- 函数 `get_dist_info`（行 91）：获取派生值、状态、路径或对象。
- 函数 `get_world_size`（行 110）：获取派生值、状态、路径或对象。
- 函数 `get_rank`（行 135）：获取派生值、状态、路径或对象。
- 函数 `is_distributed`（行 163）：提供该文件的一段辅助逻辑。
- 函数 `get_default_group`（行 167）：获取派生值、状态、路径或对象。
- 函数 `is_main_process`（行 172）：提供该文件的一段辅助逻辑。
- 函数 `master_only`（行 185）：提供该文件的一段辅助逻辑。
- 函数 `collect_results_cpu`（行 201）：提供该文件的一段辅助逻辑。
- 函数 `barrier`（行 264）：提供该文件的一段辅助逻辑。

## `projects/sa2va/evaluation/utils/grefcoco.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：330
- 类 `G_REFER`（行 35）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 37）：初始化对象状态、依赖和运行参数。
  - 方法 `_toList`（行 75）：G_REFER 的内部辅助方法。
  - 方法 `match_any`（行 79）：G_REFER 的成员方法。
  - 方法 `createIndex`（行 84）：G_REFER 的成员方法。
  - 方法 `getRefIds`（行 164）：G_REFER 的成员方法。
  - 方法 `getAnnIds`（行 186）：G_REFER 的成员方法。
  - 方法 `getImgIds`（行 206）：G_REFER 的成员方法。
  - 方法 `getCatIds`（行 215）：G_REFER 的成员方法。
  - 方法 `loadRefs`（行 218）：G_REFER 的成员方法。
  - 方法 `loadAnns`（行 221）：G_REFER 的成员方法。
  - 方法 `loadImgs`（行 226）：G_REFER 的成员方法。
  - 方法 `loadCats`（行 229）：G_REFER 的成员方法。
  - 方法 `getRefBox`（行 232）：G_REFER 的成员方法。
  - 方法 `showRef`（行 236）：G_REFER 的成员方法。
  - 方法 `getMask`（行 279）：G_REFER 的成员方法。
  - 方法 `getMaskByRef`（行 297）：G_REFER 的成员方法。
  - 方法 `showMask`（行 326）：G_REFER 的成员方法。

## `projects/sa2va/evaluation/utils/refcoco_refer.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：391
- 类 `REFER`（行 43）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 44）：初始化对象状态、依赖和运行参数。
  - 方法 `createIndex`（行 81）：REFER 的成员方法。
  - 方法 `getRefIds`（行 144）：REFER 的成员方法。
  - 方法 `getAnnIds`（行 179）：REFER 的成员方法。
  - 方法 `getImgIds`（行 205）：REFER 的成员方法。
  - 方法 `getCatIds`（行 214）：REFER 的成员方法。
  - 方法 `loadRefs`（行 217）：REFER 的成员方法。
  - 方法 `loadAnns`（行 223）：REFER 的成员方法。
  - 方法 `loadImgs`（行 229）：REFER 的成员方法。
  - 方法 `loadCats`（行 235）：REFER 的成员方法。
  - 方法 `getRefBox`（行 241）：REFER 的成员方法。
  - 方法 `showRef`（行 246）：REFER 的成员方法。
  - 方法 `getMask`（行 308）：REFER 的成员方法。
  - 方法 `showMask`（行 360）：REFER 的成员方法。

## `projects/sa2va/evaluation/utils/utils_refcoco.py`

- 文件职责：评测模块：负责数据封装、推理调度、指标计算与评估脚本。
- 行数：122
- 类 `Summary`（行 8）：类定义，承载相关状态和行为。
  - 方法：无显式方法定义。
- 类 `AverageMeter`（行 15）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 18）：初始化对象状态、依赖和运行参数。
  - 方法 `reset`（行 24）：AverageMeter 的成员方法。
  - 方法 `update`（行 30）：AverageMeter 的成员方法。
  - 方法 `all_reduce`（行 36）：AverageMeter 的成员方法。
  - 方法 `__str__`（行 59）：AverageMeter 的内部辅助方法。
  - 方法 `summary`（行 63）：AverageMeter 的成员方法。
- 类 `ProgressMeter`（行 94）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 95）：初始化对象状态、依赖和运行参数。
  - 方法 `display`（行 100）：ProgressMeter 的成员方法。
  - 方法 `display_summary`（行 105）：ProgressMeter 的成员方法。
  - 方法 `_get_batch_fmtstr`（行 110）：ProgressMeter 的内部辅助方法。
- 函数 `intersectionAndUnionGPU`（行 79）：提供该文件的一段辅助逻辑。
- 函数 `dict_to_cuda`（行 116）：提供该文件的一段辅助逻辑。

## `projects/sa2va/gradio/app.py`

- 文件职责：Gradio 可视化交互模块：提供 demo 启动和界面辅助函数。
- 行数：152
- 类 `global_infos`（行 117）：类定义，承载相关状态和行为。
  - 方法：无显式方法定义。
- 函数 `parse_args`（行 18）：解析命令行参数。
- 函数 `inference`（行 23）：提供该文件的一段辅助逻辑。
- 函数 `init_models`（行 101）：提供该文件的一段辅助逻辑。

## `projects/sa2va/gradio/app_utils.py`

- 文件职责：Gradio 可视化交互模块：提供 demo 启动和界面辅助函数。
- 行数：293
- 函数 `desaturate`（行 47）：提供该文件的一段辅助逻辑。
- 函数 `rgb_to_hls`（行 62）：提供该文件的一段辅助逻辑。
- 函数 `hls_to_rgb`（行 86）：提供该文件的一段辅助逻辑。
- 函数 `_v`（行 96）：提供该文件的一段辅助逻辑。
- 函数 `process_markdown`（行 106）：提供该文件的一段辅助逻辑。
- 函数 `show_mask_pred`（行 127）：提供该文件的一段辅助逻辑。
- 函数 `show_mask_pred_video`（行 164）：提供该文件的一段辅助逻辑。
- 函数 `parse_visual_prompts`（行 203）：提供该文件的一段辅助逻辑。
- 函数 `get_video_frames`（行 214）：获取派生值、状态、路径或对象。
- 函数 `get_frames_from_video`（行 237）：获取派生值、状态、路径或对象。
- 函数 `preprocess_video`（行 256）：提供该文件的一段辅助逻辑。
- 函数 `image2video_and_save`（行 263）：提供该文件的一段辅助逻辑。
- 函数 `frames_to_video`（行 268）：提供该文件的一段辅助逻辑。

## `projects/sa2va/hf/models/configuration_intern_vit.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：120
- 类 `InternVisionConfig`（行 16）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 64）：初始化对象状态、依赖和运行参数。
  - 方法 `from_pretrained`（行 108）：InternVisionConfig 的成员方法。

## `projects/sa2va/hf/models/configuration_internlm2.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：150
- 类 `InternLM2Config`（行 27）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 77）：初始化对象状态、依赖和运行参数。
  - 方法 `_rope_scaling_validation`（行 131）：InternLM2Config 的内部辅助方法。

## `projects/sa2va/hf/models/configuration_phi3.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：211
- 类 `Phi3Config`（行 29）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 115）：初始化对象状态、依赖和运行参数。
  - 方法 `_rope_scaling_validation`（行 173）：Phi3Config 的内部辅助方法。

## `projects/sa2va/hf/models/configuration_sa2va_chat.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：128
- 类 `Sa2VAChatConfig`（行 21）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 24）：初始化对象状态、依赖和运行参数。
  - 方法 `to_dict`（行 104）：Sa2VAChatConfig 的成员方法。

## `projects/sa2va/hf/models/flash_attention.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：76
- 类 `FlashAttention`（行 15）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 26）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 31）：执行前向计算或主推理流程。

## `projects/sa2va/hf/models/modeling_intern_vit.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：365
- 类 `InternRMSNorm`（行 33）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 34）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 39）：执行前向计算或主推理流程。
- 类 `InternVisionEmbeddings`（行 67）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 68）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_pos_embed`（行 88）：InternVisionEmbeddings 的内部辅助方法。
  - 方法 `forward`（行 96）：执行前向计算或主推理流程。
- 类 `InternAttention`（行 111）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 114）：初始化对象状态、依赖和运行参数。
  - 方法 `_naive_attn`（行 144）：InternAttention 的内部辅助方法。
  - 方法 `_flash_attn`（行 163）：InternAttention 的内部辅助方法。
  - 方法 `forward`（行 180）：执行前向计算或主推理流程。
- 类 `InternMLP`（行 185）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 186）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 193）：执行前向计算或主推理流程。
- 类 `InternVisionEncoderLayer`（行 200）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 201）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 217）：执行前向计算或主推理流程。
- 类 `InternVisionEncoder`（行 232）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 242）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 251）：执行前向计算或主推理流程。
- 类 `InternVisionModel`（行 299）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 305）：初始化对象状态、依赖和运行参数。
  - 方法 `resize_pos_embeddings`（行 312）：InternVisionModel 的成员方法。
  - 方法 `get_input_embeddings`（行 324）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 327）：执行前向计算或主推理流程。

## `projects/sa2va/hf/models/modeling_internlm2.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：1431
- 类 `InternLM2RMSNorm`（行 130）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 131）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 139）：执行前向计算或主推理流程。
- 类 `InternLM2RotaryEmbedding`（行 162）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 163）：初始化对象状态、依赖和运行参数。
  - 方法 `_set_cos_sin_cache`（行 177）：InternLM2RotaryEmbedding 的内部辅助方法。
  - 方法 `forward`（行 187）：执行前向计算或主推理流程。
- 类 `InternLM2LinearScalingRotaryEmbedding`（行 199）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 202）：初始化对象状态、依赖和运行参数。
  - 方法 `_set_cos_sin_cache`（行 206）：InternLM2LinearScalingRotaryEmbedding 的内部辅助方法。
- 类 `InternLM2DynamicNTKScalingRotaryEmbedding`（行 219）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 224）：初始化对象状态、依赖和运行参数。
  - 方法 `_set_cos_sin_cache`（行 228）：InternLM2DynamicNTKScalingRotaryEmbedding 的内部辅助方法。
- 类 `InternLM2MLP`（行 265）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 266）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 276）：执行前向计算或主推理流程。
- 类 `InternLM2Attention`（行 296）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 299）：初始化对象状态、依赖和运行参数。
  - 方法 `_init_rope`（行 325）：初始化内部子模块、配置或运行时状态。
  - 方法 `_shape`（行 353）：InternLM2Attention 的内部辅助方法。
  - 方法 `forward`（行 356）：执行前向计算或主推理流程。
- 类 `InternLM2FlashAttention2`（行 445）：类定义，承载相关状态和行为。
  - 方法 `forward`（行 452）：执行前向计算或主推理流程。
  - 方法 `_flash_attention_forward`（行 524）：InternLM2FlashAttention2 的内部辅助方法。
  - 方法 `_unpad_input`（行 578）：InternLM2FlashAttention2 的内部辅助方法。
- 类 `InternLM2DecoderLayer`（行 625）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 626）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 636）：执行前向计算或主推理流程。
- 类 `InternLM2PreTrainedModel`（行 721）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `_init_weights`（行 729）：初始化内部子模块、配置或运行时状态。
- 类 `InternLM2Model`（行 811）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 821）：初始化对象状态、依赖和运行参数。
  - 方法 `get_input_embeddings`（行 839）：获取派生值、状态、路径或对象。
  - 方法 `set_input_embeddings`（行 842）：设置运行状态、属性或随机种子。
  - 方法 `_prepare_decoder_attention_mask`（行 845）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `forward`（行 869）：执行前向计算或主推理流程。
- 类 `InternLM2ForCausalLM`（行 1004）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1009）：初始化对象状态、依赖和运行参数。
  - 方法 `get_input_embeddings`（行 1018）：获取派生值、状态、路径或对象。
  - 方法 `set_input_embeddings`（行 1021）：设置运行状态、属性或随机种子。
  - 方法 `get_output_embeddings`（行 1024）：获取派生值、状态、路径或对象。
  - 方法 `set_output_embeddings`（行 1027）：设置运行状态、属性或随机种子。
  - 方法 `set_decoder`（行 1030）：设置运行状态、属性或随机种子。
  - 方法 `get_decoder`（行 1033）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 1038）：执行前向计算或主推理流程。
  - 方法 `prepare_inputs_for_generation`（行 1128）：InternLM2ForCausalLM 的成员方法。
  - 方法 `_reorder_cache`（行 1168）：InternLM2ForCausalLM 的内部辅助方法。
  - 方法 `build_inputs`（行 1176）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `chat`（行 1189）：InternLM2ForCausalLM 的成员方法。
  - 方法 `stream_chat`（行 1225）：InternLM2ForCausalLM 的成员方法。
- 类 `InternLM2ForSequenceClassification`（行 1327）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1328）：初始化对象状态、依赖和运行参数。
  - 方法 `get_input_embeddings`（行 1337）：获取派生值、状态、路径或对象。
  - 方法 `set_input_embeddings`（行 1340）：设置运行状态、属性或随机种子。
  - 方法 `forward`（行 1344）：执行前向计算或主推理流程。
- 函数 `_import_flash_attn`（行 66）：提供该文件的一段辅助逻辑。
- 函数 `_get_unpad_data`（行 84）：提供该文件的一段辅助逻辑。
- 函数 `_make_causal_mask`（行 97）：提供该文件的一段辅助逻辑。
- 函数 `_expand_mask`（行 115）：提供该文件的一段辅助逻辑。
- 函数 `rotate_half`（行 248）：提供该文件的一段辅助逻辑。
- 函数 `apply_rotary_pos_emb`（行 256）：提供该文件的一段辅助逻辑。
- 函数 `repeat_kv`（行 283）：提供该文件的一段辅助逻辑。

## `projects/sa2va/hf/models/modeling_phi3.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：1610
- 类 `Phi3RMSNorm`（行 78）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 79）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 87）：执行前向计算或主推理流程。
- 类 `Phi3RotaryEmbedding`（行 109）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 110）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 119）：执行前向计算或主推理流程。
- 类 `Phi3SuScaledRotaryEmbedding`（行 139）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 140）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 148）：执行前向计算或主推理流程。
- 类 `Phi3YarnScaledRotaryEmbedding`（行 180）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 181）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 189）：执行前向计算或主推理流程。
- 类 `Phi3MLP`（行 257）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 258）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 267）：执行前向计算或主推理流程。
- 类 `Phi3Attention`（行 289）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 292）：初始化对象状态、依赖和运行参数。
  - 方法 `_init_rope`（行 326）：初始化内部子模块、配置或运行时状态。
  - 方法 `forward`（行 342）：执行前向计算或主推理流程。
- 类 `Phi3FlashAttention2`（行 424）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 432）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 440）：执行前向计算或主推理流程。
  - 方法 `_flash_attention_forward`（行 589）：Phi3FlashAttention2 的内部辅助方法。
  - 方法 `_upad_input`（行 690）：Phi3FlashAttention2 的内部辅助方法。
- 类 `Phi3SdpaAttention`（行 735）：类定义，承载相关状态和行为。
  - 方法 `forward`（行 743）：执行前向计算或主推理流程。
- 类 `Phi3DecoderLayer`（行 831）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 832）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 845）：执行前向计算或主推理流程。
- 类 `Phi3PreTrainedModel`（行 930）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 942）：初始化对象状态、依赖和运行参数。
  - 方法 `_init_weights`（行 948）：初始化内部子模块、配置或运行时状态。
- 类 `Phi3Model`（行 1034）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 1042）：初始化对象状态、依赖和运行参数。
  - 方法 `get_input_embeddings`（行 1060）：获取派生值、状态、路径或对象。
  - 方法 `set_input_embeddings`（行 1063）：设置运行状态、属性或随机种子。
  - 方法 `forward`（行 1067）：执行前向计算或主推理流程。
- 类 `Phi3ForCausalLM`（行 1204）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1208）：初始化对象状态、依赖和运行参数。
  - 方法 `get_input_embeddings`（行 1218）：获取派生值、状态、路径或对象。
  - 方法 `set_input_embeddings`（行 1222）：设置运行状态、属性或随机种子。
  - 方法 `get_output_embeddings`（行 1226）：获取派生值、状态、路径或对象。
  - 方法 `set_output_embeddings`（行 1230）：设置运行状态、属性或随机种子。
  - 方法 `set_decoder`（行 1234）：设置运行状态、属性或随机种子。
  - 方法 `get_decoder`（行 1238）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 1244）：执行前向计算或主推理流程。
  - 方法 `prepare_inputs_for_generation`（行 1332）：Phi3ForCausalLM 的成员方法。
  - 方法 `_reorder_cache`（行 1390）：Phi3ForCausalLM 的内部辅助方法。
- 类 `Phi3ForSequenceClassification`（行 1415）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1416）：初始化对象状态、依赖和运行参数。
  - 方法 `get_input_embeddings`（行 1425）：获取派生值、状态、路径或对象。
  - 方法 `set_input_embeddings`（行 1428）：设置运行状态、属性或随机种子。
  - 方法 `forward`（行 1432）：执行前向计算或主推理流程。
- 类 `Phi3ForTokenClassification`（行 1531）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1532）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1555）：执行前向计算或主推理流程。
- 函数 `_get_unpad_data`（行 96）：提供该文件的一段辅助逻辑。
- 函数 `rotate_half`（行 222）：提供该文件的一段辅助逻辑。
- 函数 `apply_rotary_pos_emb`（行 230）：提供该文件的一段辅助逻辑。
- 函数 `repeat_kv`（行 277）：提供该文件的一段辅助逻辑。

## `projects/sa2va/hf/models/modeling_sa2va_chat.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：897
- 类 `StopWordStoppingCriteria`（行 56）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 59）：初始化对象状态、依赖和运行参数。
  - 方法 `__call__`（行 64）：StopWordStoppingCriteria 的内部辅助方法。
- 类 `DirectResize`（行 78）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 79）：初始化对象状态、依赖和运行参数。
  - 方法 `apply_image`（行 82）：DirectResize 的成员方法。
- 类 `Sa2VAChatModel`（行 89）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 98）：初始化对象状态、依赖和运行参数。
  - 方法 `wrap_backbone_lora`（行 174）：Sa2VAChatModel 的成员方法。
  - 方法 `wrap_llm_lora`（行 184）：Sa2VAChatModel 的成员方法。
  - 方法 `pixel_shuffle`（行 206）：Sa2VAChatModel 的成员方法。
  - 方法 `extract_feature`（行 222）：Sa2VAChatModel 的成员方法。
  - 方法 `lm_head`（行 243）：Sa2VAChatModel 的成员方法。
  - 方法 `get_input_embeddings`（行 246）：获取派生值、状态、路径或对象。
  - 方法 `get_output_embeddings`（行 249）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 252）：执行前向计算或主推理流程。
  - 方法 `_llm_forward`（行 301）：Sa2VAChatModel 的内部辅助方法。
  - 方法 `generate`（行 435）：Sa2VAChatModel 的成员方法。
  - 方法 `preparing_for_generation`（行 527）：Sa2VAChatModel 的成员方法。
  - 方法 `predict_forward`（行 589）：Sa2VAChatModel 的成员方法。
- 函数 `version_cmp`（行 49）：提供该文件的一段辅助逻辑。
- 函数 `get_stop_criteria`（行 69）：获取派生值、状态、路径或对象。
- 函数 `get_seg_hidden_states`（行 775）：获取派生值、状态、路径或对象。
- 函数 `find_closest_aspect_ratio`（行 782）：搜索最匹配的对象、索引或路径。
- 函数 `dynamic_preprocess`（行 798）：提供该文件的一段辅助逻辑。
- 函数 `prepare_inputs_for_generation_phi3`（行 843）：提供该文件的一段辅助逻辑。

## `projects/sa2va/hf/models/sam2.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：4103
- 类 `SAM2`（行 76）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 77）：初始化对象状态、依赖和运行参数。
  - 方法 `build_image_encoder`（行 137）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `build_memory_attention`（行 186）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `build_memory_encoder`（行 257）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `inject_language_embd`（行 316）：SAM2 的成员方法。
  - 方法 `language_embd_inference`（行 332）：SAM2 的成员方法。
  - 方法 `get_sam2_embeddings`（行 360）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 363）：执行前向计算或主推理流程。
  - 方法 `preprocess_image`（行 366）：SAM2 的成员方法。
- 类 `MemoryAttentionLayer`（行 376）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 378）：初始化对象状态、依赖和运行参数。
  - 方法 `_forward_sa`（行 417）：MemoryAttentionLayer 的内部辅助方法。
  - 方法 `_forward_ca`（行 425）：MemoryAttentionLayer 的内部辅助方法。
  - 方法 `forward`（行 442）：执行前向计算或主推理流程。
- 类 `MemoryAttention`（行 461）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 462）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 478）：执行前向计算或主推理流程。
- 类 `MaskDownSampler`（行 530）：采样器类，控制迭代顺序、分组和分布式切分。
  - 方法 `__init__`（行 539）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 570）：执行前向计算或主推理流程。
- 类 `CXBlock`（行 575）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 587）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 618）：执行前向计算或主推理流程。
- 类 `Fuser`（行 634）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 635）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 644）：执行前向计算或主推理流程。
- 类 `MemoryEncoder`（行 652）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 653）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 672）：执行前向计算或主推理流程。
- 类 `ImageEncoder`（行 698）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 699）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 713）：执行前向计算或主推理流程。
- 类 `FpnNeck`（行 729）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 736）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 785）：执行前向计算或主推理流程。
- 类 `PatchEmbed`（行 868）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 873）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 894）：执行前向计算或主推理流程。
- 类 `MultiScaleAttention`（行 914）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 915）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 935）：执行前向计算或主推理流程。
- 类 `MultiScaleBlock`（行 963）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 964）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1013）：执行前向计算或主推理流程。
- 类 `Hiera`（行 1048）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1053）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_pos_embed`（行 1146）：Hiera 的内部辅助方法。
  - 方法 `forward`（行 1156）：执行前向计算或主推理流程。
- 类 `TwoWayTransformer`（行 1174）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1175）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1220）：执行前向计算或主推理流程。
- 类 `TwoWayAttentionBlock`（行 1267）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1268）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1311）：执行前向计算或主推理流程。
- 类 `Attention`（行 1345）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1351）：初始化对象状态、依赖和运行参数。
  - 方法 `_separate_heads`（行 1375）：Attention 的内部辅助方法。
  - 方法 `_recombine_heads`（行 1380）：Attention 的内部辅助方法。
  - 方法 `forward`（行 1385）：执行前向计算或主推理流程。
- 类 `RoPEAttention`（行 1412）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1415）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1434）：执行前向计算或主推理流程。
- 类 `PromptEncoder`（行 1479）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1480）：初始化对象状态、依赖和运行参数。
  - 方法 `get_dense_pe`（行 1530）：获取派生值、状态、路径或对象。
  - 方法 `_embed_points`（行 1541）：PromptEncoder 的内部辅助方法。
  - 方法 `_embed_boxes`（行 1565）：PromptEncoder 的内部辅助方法。
  - 方法 `_embed_masks`（行 1576）：PromptEncoder 的内部辅助方法。
  - 方法 `_get_batch_size`（行 1581）：PromptEncoder 的内部辅助方法。
  - 方法 `_get_device`（行 1599）：PromptEncoder 的内部辅助方法。
  - 方法 `forward`（行 1602）：执行前向计算或主推理流程。
- 类 `PositionEmbeddingSine`（行 1646）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1652）：初始化对象状态、依赖和运行参数。
  - 方法 `_encode_xy`（行 1672）：PositionEmbeddingSine 的内部辅助方法。
  - 方法 `encode_boxes`（行 1692）：把对象编码为模型或存储所需格式。
  - 方法 `encode_points`（行 1700）：把对象编码为模型或存储所需格式。
  - 方法 `forward`（行 1709）：执行前向计算或主推理流程。
- 类 `PositionEmbeddingRandom`（行 1745）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1750）：初始化对象状态、依赖和运行参数。
  - 方法 `_pe_encoding`（行 1760）：PositionEmbeddingRandom 的内部辅助方法。
  - 方法 `forward`（行 1773）：执行前向计算或主推理流程。
  - 方法 `forward_with_coords`（行 1786）：PositionEmbeddingRandom 的成员方法。
- 类 `MaskDecoder`（行 1854）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1855）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1949）：执行前向计算或主推理流程。
  - 方法 `predict_masks`（行 2007）：MaskDecoder 的成员方法。
  - 方法 `_get_stability_scores`（行 2090）：MaskDecoder 的内部辅助方法。
  - 方法 `_dynamic_multimask_via_stability`（行 2102）：MaskDecoder 的内部辅助方法。
- 类 `DropPath`（行 2213）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2215）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2220）：执行前向计算或主推理流程。
- 类 `MLP`（行 2233）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2234）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2252）：执行前向计算或主推理流程。
- 类 `LayerNorm2d`（行 2262）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2263）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2269）：执行前向计算或主推理流程。
- 类 `SAM2Base_`（行 2276）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2277）：初始化对象状态、依赖和运行参数。
  - 方法 `device`（行 2446）：SAM2Base_ 的成员方法。
  - 方法 `forward`（行 2449）：执行前向计算或主推理流程。
  - 方法 `_build_sam_heads`（行 2455）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_forward_sam_heads`（行 2505）：SAM2Base_ 的内部辅助方法。
  - 方法 `_use_mask_as_output`（行 2666）：SAM2Base_ 的内部辅助方法。
  - 方法 `forward_image`（行 2718）：SAM2Base_ 的成员方法。
  - 方法 `_prepare_backbone_features`（行 2732）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_prepare_memory_conditioned_features`（行 2748）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_encode_new_memory`（行 2919）：SAM2Base_ 的内部辅助方法。
  - 方法 `track_step`（行 2959）：SAM2Base_ 的成员方法。
  - 方法 `_use_multimask`（行 3056）：SAM2Base_ 的内部辅助方法。
  - 方法 `_apply_non_overlapping_constraints`（行 3066）：SAM2Base_ 的内部辅助方法。
- 类 `SAM2Base`（行 3086）：类定义，承载相关状态和行为。
  - 方法 `track_step`（行 3088）：SAM2Base 的成员方法。
  - 方法 `_forward_sam_heads`（行 3190）：SAM2Base 的内部辅助方法。
- 类 `SAM2VideoPredictor`（行 3433）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 3436）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_image_feature`（行 3454）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_single_frame_inference`（行 3488）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_consolidate_temp_output_across_obj`（行 3558）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_get_orig_video_res_output`（行 3677）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `init_state`（行 3699）：SAM2VideoPredictor 的成员方法。
  - 方法 `add_language_embd`（行 3752）：SAM2VideoPredictor 的成员方法。
  - 方法 `_clear_non_cond_mem_around_input`（行 3829）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_memory_encoder`（行 3848）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_add_output_per_object`（行 3877）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `propagate_in_video_preflight`（行 3906）：SAM2VideoPredictor 的成员方法。
  - 方法 `propagate_in_video`（行 3978）：SAM2VideoPredictor 的成员方法。
- 函数 `load_checkpoint_with_prefix`（行 29）：从文件或配置中加载数据。
- 函数 `load_state_dict_to_model`（行 66）：从文件或配置中加载数据。
- 函数 `window_partition`（行 819）：提供该文件的一段辅助逻辑。
- 函数 `window_unpartition`（行 844）：提供该文件的一段辅助逻辑。
- 函数 `do_pool`（行 900）：提供该文件的一段辅助逻辑。
- 函数 `init_t_xy`（行 1802）：提供该文件的一段辅助逻辑。
- 函数 `compute_axial_cis`（行 1809）：提供该文件的一段辅助逻辑。
- 函数 `reshape_for_broadcast`（行 1821）：提供该文件的一段辅助逻辑。
- 函数 `apply_rotary_enc`（行 1829）：提供该文件的一段辅助逻辑。
- 函数 `select_closest_cond_frames`（行 2140）：提供该文件的一段辅助逻辑。
- 函数 `get_1d_sine_pe`（行 2185）：获取派生值、状态、路径或对象。
- 函数 `get_activation_fn`（行 2198）：获取派生值、状态、路径或对象。
- 函数 `get_clones`（行 2209）：获取派生值、状态、路径或对象。
- 函数 `_obj_id_to_idx`（行 3362）：提供该文件的一段辅助逻辑。
- 函数 `_get_maskmem_pos_enc`（行 3397）：提供该文件的一段辅助逻辑。
- 函数 `_obj_idx_to_id`（行 3423）：提供该文件的一段辅助逻辑。
- 函数 `_get_obj_num`（行 3428）：提供该文件的一段辅助逻辑。
- 函数 `fill_holes_in_mask_scores`（行 4062）：提供该文件的一段辅助逻辑。
- 函数 `get_connected_components`（行 4075）：获取派生值、状态、路径或对象。

## `projects/sa2va/hf/models/templates.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：170
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/hf/models/tokenization_internlm2.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：235
- 类 `InternLM2Tokenizer`（行 34）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 48）：初始化对象状态、依赖和运行参数。
  - 方法 `no_prefix_space_tokens`（行 80）：InternLM2Tokenizer 的成员方法。
  - 方法 `vocab_size`（行 87）：InternLM2Tokenizer 的成员方法。
  - 方法 `bos_token_id`（行 92）：InternLM2Tokenizer 的成员方法。
  - 方法 `eos_token_id`（行 96）：InternLM2Tokenizer 的成员方法。
  - 方法 `get_vocab`（行 99）：获取派生值、状态、路径或对象。
  - 方法 `_tokenize`（行 105）：InternLM2Tokenizer 的内部辅助方法。
  - 方法 `_convert_token_to_id`（行 109）：InternLM2Tokenizer 的内部辅助方法。
  - 方法 `_convert_id_to_token`（行 113）：InternLM2Tokenizer 的内部辅助方法。
  - 方法 `_maybe_add_prefix_space`（行 118）：InternLM2Tokenizer 的内部辅助方法。
  - 方法 `convert_tokens_to_string`（行 124）：InternLM2Tokenizer 的成员方法。
  - 方法 `save_vocabulary`（行 145）：把结果写回磁盘或序列化输出。
  - 方法 `build_inputs_with_special_tokens`（行 172）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `get_special_tokens_mask`（行 188）：获取派生值、状态、路径或对象。
  - 方法 `create_token_type_ids_from_sequences`（行 215）：InternLM2Tokenizer 的成员方法。

## `projects/sa2va/hf/models/tokenization_internlm2_fast.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：211
- 类 `InternLM2Converter`（行 38）：类定义，承载相关状态和行为。
  - 方法 `vocab`（行 41）：InternLM2Converter 的成员方法。
  - 方法 `unk_id`（行 50）：InternLM2Converter 的成员方法。
  - 方法 `decoder`（行 54）：InternLM2Converter 的成员方法。
  - 方法 `tokenizer`（行 64）：InternLM2Converter 的成员方法。
  - 方法 `normalizer`（行 92）：InternLM2Converter 的成员方法。
  - 方法 `pre_tokenizer`（行 99）：InternLM2Converter 的成员方法。
- 类 `InternLM2TokenizerFast`（行 107）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 114）：初始化对象状态、依赖和运行参数。
  - 方法 `can_save_slow_tokenizer`（行 147）：InternLM2TokenizerFast 的成员方法。
  - 方法 `update_post_processor`（行 150）：InternLM2TokenizerFast 的成员方法。
  - 方法 `add_eos_token`（行 177）：InternLM2TokenizerFast 的成员方法。
  - 方法 `add_bos_token`（行 181）：InternLM2TokenizerFast 的成员方法。
  - 方法 `add_eos_token`（行 185）：InternLM2TokenizerFast 的成员方法。
  - 方法 `add_bos_token`（行 190）：InternLM2TokenizerFast 的成员方法。
  - 方法 `save_vocabulary`（行 194）：把结果写回磁盘或序列化输出。

## `projects/sa2va/hf/models_qwen/configuration_sa2va_chat.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：34
- 类 `Sa2VAChatConfigQwen`（行 12）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 15）：初始化对象状态、依赖和运行参数。
  - 方法 `to_dict`（行 23）：Sa2VAChatConfigQwen 的成员方法。

## `projects/sa2va/hf/models_qwen/modeling_sa2va_qwen.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：260
- 类 `DirectResize`（行 20）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 21）：初始化对象状态、依赖和运行参数。
  - 方法 `apply_image`（行 24）：DirectResize 的成员方法。
- 类 `Sa2VAChatModelQwen`（行 31）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 41）：初始化对象状态、依赖和运行参数。
  - 方法 `lm_head`（行 66）：Sa2VAChatModelQwen 的成员方法。
  - 方法 `get_input_embeddings`（行 69）：获取派生值、状态、路径或对象。
  - 方法 `get_output_embeddings`（行 72）：获取派生值、状态、路径或对象。
  - 方法 `predict_forward`（行 75）：Sa2VAChatModelQwen 的成员方法。
- 函数 `get_seg_hidden_states`（行 255）：获取派生值、状态、路径或对象。

## `projects/sa2va/hf/models_qwen/sam2.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：4103
- 类 `SAM2`（行 76）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 77）：初始化对象状态、依赖和运行参数。
  - 方法 `build_image_encoder`（行 137）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `build_memory_attention`（行 186）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `build_memory_encoder`（行 257）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `inject_language_embd`（行 316）：SAM2 的成员方法。
  - 方法 `language_embd_inference`（行 332）：SAM2 的成员方法。
  - 方法 `get_sam2_embeddings`（行 360）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 363）：执行前向计算或主推理流程。
  - 方法 `preprocess_image`（行 366）：SAM2 的成员方法。
- 类 `MemoryAttentionLayer`（行 376）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 378）：初始化对象状态、依赖和运行参数。
  - 方法 `_forward_sa`（行 417）：MemoryAttentionLayer 的内部辅助方法。
  - 方法 `_forward_ca`（行 425）：MemoryAttentionLayer 的内部辅助方法。
  - 方法 `forward`（行 442）：执行前向计算或主推理流程。
- 类 `MemoryAttention`（行 461）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 462）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 478）：执行前向计算或主推理流程。
- 类 `MaskDownSampler`（行 530）：采样器类，控制迭代顺序、分组和分布式切分。
  - 方法 `__init__`（行 539）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 570）：执行前向计算或主推理流程。
- 类 `CXBlock`（行 575）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 587）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 618）：执行前向计算或主推理流程。
- 类 `Fuser`（行 634）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 635）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 644）：执行前向计算或主推理流程。
- 类 `MemoryEncoder`（行 652）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 653）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 672）：执行前向计算或主推理流程。
- 类 `ImageEncoder`（行 698）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 699）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 713）：执行前向计算或主推理流程。
- 类 `FpnNeck`（行 729）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 736）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 785）：执行前向计算或主推理流程。
- 类 `PatchEmbed`（行 868）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 873）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 894）：执行前向计算或主推理流程。
- 类 `MultiScaleAttention`（行 914）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 915）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 935）：执行前向计算或主推理流程。
- 类 `MultiScaleBlock`（行 963）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 964）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1013）：执行前向计算或主推理流程。
- 类 `Hiera`（行 1048）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1053）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_pos_embed`（行 1146）：Hiera 的内部辅助方法。
  - 方法 `forward`（行 1156）：执行前向计算或主推理流程。
- 类 `TwoWayTransformer`（行 1174）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1175）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1220）：执行前向计算或主推理流程。
- 类 `TwoWayAttentionBlock`（行 1267）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1268）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1311）：执行前向计算或主推理流程。
- 类 `Attention`（行 1345）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1351）：初始化对象状态、依赖和运行参数。
  - 方法 `_separate_heads`（行 1375）：Attention 的内部辅助方法。
  - 方法 `_recombine_heads`（行 1380）：Attention 的内部辅助方法。
  - 方法 `forward`（行 1385）：执行前向计算或主推理流程。
- 类 `RoPEAttention`（行 1412）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1415）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1434）：执行前向计算或主推理流程。
- 类 `PromptEncoder`（行 1479）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1480）：初始化对象状态、依赖和运行参数。
  - 方法 `get_dense_pe`（行 1530）：获取派生值、状态、路径或对象。
  - 方法 `_embed_points`（行 1541）：PromptEncoder 的内部辅助方法。
  - 方法 `_embed_boxes`（行 1565）：PromptEncoder 的内部辅助方法。
  - 方法 `_embed_masks`（行 1576）：PromptEncoder 的内部辅助方法。
  - 方法 `_get_batch_size`（行 1581）：PromptEncoder 的内部辅助方法。
  - 方法 `_get_device`（行 1599）：PromptEncoder 的内部辅助方法。
  - 方法 `forward`（行 1602）：执行前向计算或主推理流程。
- 类 `PositionEmbeddingSine`（行 1646）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1652）：初始化对象状态、依赖和运行参数。
  - 方法 `_encode_xy`（行 1672）：PositionEmbeddingSine 的内部辅助方法。
  - 方法 `encode_boxes`（行 1692）：把对象编码为模型或存储所需格式。
  - 方法 `encode_points`（行 1700）：把对象编码为模型或存储所需格式。
  - 方法 `forward`（行 1709）：执行前向计算或主推理流程。
- 类 `PositionEmbeddingRandom`（行 1745）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1750）：初始化对象状态、依赖和运行参数。
  - 方法 `_pe_encoding`（行 1760）：PositionEmbeddingRandom 的内部辅助方法。
  - 方法 `forward`（行 1773）：执行前向计算或主推理流程。
  - 方法 `forward_with_coords`（行 1786）：PositionEmbeddingRandom 的成员方法。
- 类 `MaskDecoder`（行 1854）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1855）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1949）：执行前向计算或主推理流程。
  - 方法 `predict_masks`（行 2007）：MaskDecoder 的成员方法。
  - 方法 `_get_stability_scores`（行 2090）：MaskDecoder 的内部辅助方法。
  - 方法 `_dynamic_multimask_via_stability`（行 2102）：MaskDecoder 的内部辅助方法。
- 类 `DropPath`（行 2213）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2215）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2220）：执行前向计算或主推理流程。
- 类 `MLP`（行 2233）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2234）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2252）：执行前向计算或主推理流程。
- 类 `LayerNorm2d`（行 2262）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2263）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2269）：执行前向计算或主推理流程。
- 类 `SAM2Base_`（行 2276）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2277）：初始化对象状态、依赖和运行参数。
  - 方法 `device`（行 2446）：SAM2Base_ 的成员方法。
  - 方法 `forward`（行 2449）：执行前向计算或主推理流程。
  - 方法 `_build_sam_heads`（行 2455）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_forward_sam_heads`（行 2505）：SAM2Base_ 的内部辅助方法。
  - 方法 `_use_mask_as_output`（行 2666）：SAM2Base_ 的内部辅助方法。
  - 方法 `forward_image`（行 2718）：SAM2Base_ 的成员方法。
  - 方法 `_prepare_backbone_features`（行 2732）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_prepare_memory_conditioned_features`（行 2748）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_encode_new_memory`（行 2919）：SAM2Base_ 的内部辅助方法。
  - 方法 `track_step`（行 2959）：SAM2Base_ 的成员方法。
  - 方法 `_use_multimask`（行 3056）：SAM2Base_ 的内部辅助方法。
  - 方法 `_apply_non_overlapping_constraints`（行 3066）：SAM2Base_ 的内部辅助方法。
- 类 `SAM2Base`（行 3086）：类定义，承载相关状态和行为。
  - 方法 `track_step`（行 3088）：SAM2Base 的成员方法。
  - 方法 `_forward_sam_heads`（行 3190）：SAM2Base 的内部辅助方法。
- 类 `SAM2VideoPredictor`（行 3433）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 3436）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_image_feature`（行 3454）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_single_frame_inference`（行 3488）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_consolidate_temp_output_across_obj`（行 3558）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_get_orig_video_res_output`（行 3677）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `init_state`（行 3699）：SAM2VideoPredictor 的成员方法。
  - 方法 `add_language_embd`（行 3752）：SAM2VideoPredictor 的成员方法。
  - 方法 `_clear_non_cond_mem_around_input`（行 3829）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_memory_encoder`（行 3848）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_add_output_per_object`（行 3877）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `propagate_in_video_preflight`（行 3906）：SAM2VideoPredictor 的成员方法。
  - 方法 `propagate_in_video`（行 3978）：SAM2VideoPredictor 的成员方法。
- 函数 `load_checkpoint_with_prefix`（行 29）：从文件或配置中加载数据。
- 函数 `load_state_dict_to_model`（行 66）：从文件或配置中加载数据。
- 函数 `window_partition`（行 819）：提供该文件的一段辅助逻辑。
- 函数 `window_unpartition`（行 844）：提供该文件的一段辅助逻辑。
- 函数 `do_pool`（行 900）：提供该文件的一段辅助逻辑。
- 函数 `init_t_xy`（行 1802）：提供该文件的一段辅助逻辑。
- 函数 `compute_axial_cis`（行 1809）：提供该文件的一段辅助逻辑。
- 函数 `reshape_for_broadcast`（行 1821）：提供该文件的一段辅助逻辑。
- 函数 `apply_rotary_enc`（行 1829）：提供该文件的一段辅助逻辑。
- 函数 `select_closest_cond_frames`（行 2140）：提供该文件的一段辅助逻辑。
- 函数 `get_1d_sine_pe`（行 2185）：获取派生值、状态、路径或对象。
- 函数 `get_activation_fn`（行 2198）：获取派生值、状态、路径或对象。
- 函数 `get_clones`（行 2209）：获取派生值、状态、路径或对象。
- 函数 `_obj_id_to_idx`（行 3362）：提供该文件的一段辅助逻辑。
- 函数 `_get_maskmem_pos_enc`（行 3397）：提供该文件的一段辅助逻辑。
- 函数 `_obj_idx_to_id`（行 3423）：提供该文件的一段辅助逻辑。
- 函数 `_get_obj_num`（行 3428）：提供该文件的一段辅助逻辑。
- 函数 `fill_holes_in_mask_scores`（行 4062）：提供该文件的一段辅助逻辑。
- 函数 `get_connected_components`（行 4075）：获取派生值、状态、路径或对象。

## `projects/sa2va/hf/models_qwen/templates.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：170
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/hf/models_qwen2_5_vl/configuration_sa2va_chat.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：34
- 类 `Sa2VAChatConfigQwen`（行 12）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 15）：初始化对象状态、依赖和运行参数。
  - 方法 `to_dict`（行 23）：Sa2VAChatConfigQwen 的成员方法。

## `projects/sa2va/hf/models_qwen2_5_vl/modeling_sa2va_qwen.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：390
- 类 `DirectResize`（行 20）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 21）：初始化对象状态、依赖和运行参数。
  - 方法 `apply_image`（行 24）：DirectResize 的成员方法。
- 类 `Sa2VAChatModelQwen`（行 31）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 41）：初始化对象状态、依赖和运行参数。
  - 方法 `lm_head`（行 66）：Sa2VAChatModelQwen 的成员方法。
  - 方法 `get_input_embeddings`（行 69）：获取派生值、状态、路径或对象。
  - 方法 `get_output_embeddings`（行 72）：获取派生值、状态、路径或对象。
  - 方法 `_normalize_mask_prompts`（行 76）：Sa2VAChatModelQwen 的内部辅助方法。
  - 方法 `_resize_mask_prompts`（行 87）：Sa2VAChatModelQwen 的内部辅助方法。
  - 方法 `_build_visual_prompt_text`（行 113）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_inject_visual_prompt_embeds`（行 123）：Sa2VAChatModelQwen 的内部辅助方法。
  - 方法 `predict_forward`（行 160）：Sa2VAChatModelQwen 的成员方法。
- 函数 `get_seg_hidden_states`（行 385）：获取派生值、状态、路径或对象。

## `projects/sa2va/hf/models_qwen2_5_vl/sam2.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：4103
- 类 `SAM2`（行 76）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 77）：初始化对象状态、依赖和运行参数。
  - 方法 `build_image_encoder`（行 137）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `build_memory_attention`（行 186）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `build_memory_encoder`（行 257）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `inject_language_embd`（行 316）：SAM2 的成员方法。
  - 方法 `language_embd_inference`（行 332）：SAM2 的成员方法。
  - 方法 `get_sam2_embeddings`（行 360）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 363）：执行前向计算或主推理流程。
  - 方法 `preprocess_image`（行 366）：SAM2 的成员方法。
- 类 `MemoryAttentionLayer`（行 376）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 378）：初始化对象状态、依赖和运行参数。
  - 方法 `_forward_sa`（行 417）：MemoryAttentionLayer 的内部辅助方法。
  - 方法 `_forward_ca`（行 425）：MemoryAttentionLayer 的内部辅助方法。
  - 方法 `forward`（行 442）：执行前向计算或主推理流程。
- 类 `MemoryAttention`（行 461）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 462）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 478）：执行前向计算或主推理流程。
- 类 `MaskDownSampler`（行 530）：采样器类，控制迭代顺序、分组和分布式切分。
  - 方法 `__init__`（行 539）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 570）：执行前向计算或主推理流程。
- 类 `CXBlock`（行 575）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 587）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 618）：执行前向计算或主推理流程。
- 类 `Fuser`（行 634）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 635）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 644）：执行前向计算或主推理流程。
- 类 `MemoryEncoder`（行 652）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 653）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 672）：执行前向计算或主推理流程。
- 类 `ImageEncoder`（行 698）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 699）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 713）：执行前向计算或主推理流程。
- 类 `FpnNeck`（行 729）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 736）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 785）：执行前向计算或主推理流程。
- 类 `PatchEmbed`（行 868）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 873）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 894）：执行前向计算或主推理流程。
- 类 `MultiScaleAttention`（行 914）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 915）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 935）：执行前向计算或主推理流程。
- 类 `MultiScaleBlock`（行 963）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 964）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1013）：执行前向计算或主推理流程。
- 类 `Hiera`（行 1048）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1053）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_pos_embed`（行 1146）：Hiera 的内部辅助方法。
  - 方法 `forward`（行 1156）：执行前向计算或主推理流程。
- 类 `TwoWayTransformer`（行 1174）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1175）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1220）：执行前向计算或主推理流程。
- 类 `TwoWayAttentionBlock`（行 1267）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1268）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1311）：执行前向计算或主推理流程。
- 类 `Attention`（行 1345）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1351）：初始化对象状态、依赖和运行参数。
  - 方法 `_separate_heads`（行 1375）：Attention 的内部辅助方法。
  - 方法 `_recombine_heads`（行 1380）：Attention 的内部辅助方法。
  - 方法 `forward`（行 1385）：执行前向计算或主推理流程。
- 类 `RoPEAttention`（行 1412）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1415）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1434）：执行前向计算或主推理流程。
- 类 `PromptEncoder`（行 1479）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1480）：初始化对象状态、依赖和运行参数。
  - 方法 `get_dense_pe`（行 1530）：获取派生值、状态、路径或对象。
  - 方法 `_embed_points`（行 1541）：PromptEncoder 的内部辅助方法。
  - 方法 `_embed_boxes`（行 1565）：PromptEncoder 的内部辅助方法。
  - 方法 `_embed_masks`（行 1576）：PromptEncoder 的内部辅助方法。
  - 方法 `_get_batch_size`（行 1581）：PromptEncoder 的内部辅助方法。
  - 方法 `_get_device`（行 1599）：PromptEncoder 的内部辅助方法。
  - 方法 `forward`（行 1602）：执行前向计算或主推理流程。
- 类 `PositionEmbeddingSine`（行 1646）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1652）：初始化对象状态、依赖和运行参数。
  - 方法 `_encode_xy`（行 1672）：PositionEmbeddingSine 的内部辅助方法。
  - 方法 `encode_boxes`（行 1692）：把对象编码为模型或存储所需格式。
  - 方法 `encode_points`（行 1700）：把对象编码为模型或存储所需格式。
  - 方法 `forward`（行 1709）：执行前向计算或主推理流程。
- 类 `PositionEmbeddingRandom`（行 1745）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1750）：初始化对象状态、依赖和运行参数。
  - 方法 `_pe_encoding`（行 1760）：PositionEmbeddingRandom 的内部辅助方法。
  - 方法 `forward`（行 1773）：执行前向计算或主推理流程。
  - 方法 `forward_with_coords`（行 1786）：PositionEmbeddingRandom 的成员方法。
- 类 `MaskDecoder`（行 1854）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1855）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1949）：执行前向计算或主推理流程。
  - 方法 `predict_masks`（行 2007）：MaskDecoder 的成员方法。
  - 方法 `_get_stability_scores`（行 2090）：MaskDecoder 的内部辅助方法。
  - 方法 `_dynamic_multimask_via_stability`（行 2102）：MaskDecoder 的内部辅助方法。
- 类 `DropPath`（行 2213）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2215）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2220）：执行前向计算或主推理流程。
- 类 `MLP`（行 2233）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2234）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2252）：执行前向计算或主推理流程。
- 类 `LayerNorm2d`（行 2262）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2263）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2269）：执行前向计算或主推理流程。
- 类 `SAM2Base_`（行 2276）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2277）：初始化对象状态、依赖和运行参数。
  - 方法 `device`（行 2446）：SAM2Base_ 的成员方法。
  - 方法 `forward`（行 2449）：执行前向计算或主推理流程。
  - 方法 `_build_sam_heads`（行 2455）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_forward_sam_heads`（行 2505）：SAM2Base_ 的内部辅助方法。
  - 方法 `_use_mask_as_output`（行 2666）：SAM2Base_ 的内部辅助方法。
  - 方法 `forward_image`（行 2718）：SAM2Base_ 的成员方法。
  - 方法 `_prepare_backbone_features`（行 2732）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_prepare_memory_conditioned_features`（行 2748）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_encode_new_memory`（行 2919）：SAM2Base_ 的内部辅助方法。
  - 方法 `track_step`（行 2959）：SAM2Base_ 的成员方法。
  - 方法 `_use_multimask`（行 3056）：SAM2Base_ 的内部辅助方法。
  - 方法 `_apply_non_overlapping_constraints`（行 3066）：SAM2Base_ 的内部辅助方法。
- 类 `SAM2Base`（行 3086）：类定义，承载相关状态和行为。
  - 方法 `track_step`（行 3088）：SAM2Base 的成员方法。
  - 方法 `_forward_sam_heads`（行 3190）：SAM2Base 的内部辅助方法。
- 类 `SAM2VideoPredictor`（行 3433）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 3436）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_image_feature`（行 3454）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_single_frame_inference`（行 3488）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_consolidate_temp_output_across_obj`（行 3558）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_get_orig_video_res_output`（行 3677）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `init_state`（行 3699）：SAM2VideoPredictor 的成员方法。
  - 方法 `add_language_embd`（行 3752）：SAM2VideoPredictor 的成员方法。
  - 方法 `_clear_non_cond_mem_around_input`（行 3829）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_memory_encoder`（行 3848）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_add_output_per_object`（行 3877）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `propagate_in_video_preflight`（行 3906）：SAM2VideoPredictor 的成员方法。
  - 方法 `propagate_in_video`（行 3978）：SAM2VideoPredictor 的成员方法。
- 函数 `load_checkpoint_with_prefix`（行 29）：从文件或配置中加载数据。
- 函数 `load_state_dict_to_model`（行 66）：从文件或配置中加载数据。
- 函数 `window_partition`（行 819）：提供该文件的一段辅助逻辑。
- 函数 `window_unpartition`（行 844）：提供该文件的一段辅助逻辑。
- 函数 `do_pool`（行 900）：提供该文件的一段辅助逻辑。
- 函数 `init_t_xy`（行 1802）：提供该文件的一段辅助逻辑。
- 函数 `compute_axial_cis`（行 1809）：提供该文件的一段辅助逻辑。
- 函数 `reshape_for_broadcast`（行 1821）：提供该文件的一段辅助逻辑。
- 函数 `apply_rotary_enc`（行 1829）：提供该文件的一段辅助逻辑。
- 函数 `select_closest_cond_frames`（行 2140）：提供该文件的一段辅助逻辑。
- 函数 `get_1d_sine_pe`（行 2185）：获取派生值、状态、路径或对象。
- 函数 `get_activation_fn`（行 2198）：获取派生值、状态、路径或对象。
- 函数 `get_clones`（行 2209）：获取派生值、状态、路径或对象。
- 函数 `_obj_id_to_idx`（行 3362）：提供该文件的一段辅助逻辑。
- 函数 `_get_maskmem_pos_enc`（行 3397）：提供该文件的一段辅助逻辑。
- 函数 `_obj_idx_to_id`（行 3423）：提供该文件的一段辅助逻辑。
- 函数 `_get_obj_num`（行 3428）：提供该文件的一段辅助逻辑。
- 函数 `fill_holes_in_mask_scores`（行 4062）：提供该文件的一段辅助逻辑。
- 函数 `get_connected_components`（行 4075）：获取派生值、状态、路径或对象。

## `projects/sa2va/hf/models_qwen2_5_vl/templates.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：170
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/hf/models_qwen3vl/configuration_sa2va_chat.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：34
- 类 `Sa2VAChatConfigQwen`（行 12）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 15）：初始化对象状态、依赖和运行参数。
  - 方法 `to_dict`（行 23）：Sa2VAChatConfigQwen 的成员方法。

## `projects/sa2va/hf/models_qwen3vl/modeling_sa2va_qwen.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：260
- 类 `DirectResize`（行 20）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 21）：初始化对象状态、依赖和运行参数。
  - 方法 `apply_image`（行 24）：DirectResize 的成员方法。
- 类 `Sa2VAChatModelQwen`（行 31）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 41）：初始化对象状态、依赖和运行参数。
  - 方法 `lm_head`（行 66）：Sa2VAChatModelQwen 的成员方法。
  - 方法 `get_input_embeddings`（行 69）：获取派生值、状态、路径或对象。
  - 方法 `get_output_embeddings`（行 72）：获取派生值、状态、路径或对象。
  - 方法 `predict_forward`（行 75）：Sa2VAChatModelQwen 的成员方法。
- 函数 `get_seg_hidden_states`（行 255）：获取派生值、状态、路径或对象。

## `projects/sa2va/hf/models_qwen3vl/sam2.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：4103
- 类 `SAM2`（行 76）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 77）：初始化对象状态、依赖和运行参数。
  - 方法 `build_image_encoder`（行 137）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `build_memory_attention`（行 186）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `build_memory_encoder`（行 257）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `inject_language_embd`（行 316）：SAM2 的成员方法。
  - 方法 `language_embd_inference`（行 332）：SAM2 的成员方法。
  - 方法 `get_sam2_embeddings`（行 360）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 363）：执行前向计算或主推理流程。
  - 方法 `preprocess_image`（行 366）：SAM2 的成员方法。
- 类 `MemoryAttentionLayer`（行 376）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 378）：初始化对象状态、依赖和运行参数。
  - 方法 `_forward_sa`（行 417）：MemoryAttentionLayer 的内部辅助方法。
  - 方法 `_forward_ca`（行 425）：MemoryAttentionLayer 的内部辅助方法。
  - 方法 `forward`（行 442）：执行前向计算或主推理流程。
- 类 `MemoryAttention`（行 461）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 462）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 478）：执行前向计算或主推理流程。
- 类 `MaskDownSampler`（行 530）：采样器类，控制迭代顺序、分组和分布式切分。
  - 方法 `__init__`（行 539）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 570）：执行前向计算或主推理流程。
- 类 `CXBlock`（行 575）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 587）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 618）：执行前向计算或主推理流程。
- 类 `Fuser`（行 634）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 635）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 644）：执行前向计算或主推理流程。
- 类 `MemoryEncoder`（行 652）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 653）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 672）：执行前向计算或主推理流程。
- 类 `ImageEncoder`（行 698）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 699）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 713）：执行前向计算或主推理流程。
- 类 `FpnNeck`（行 729）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 736）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 785）：执行前向计算或主推理流程。
- 类 `PatchEmbed`（行 868）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 873）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 894）：执行前向计算或主推理流程。
- 类 `MultiScaleAttention`（行 914）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 915）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 935）：执行前向计算或主推理流程。
- 类 `MultiScaleBlock`（行 963）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 964）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1013）：执行前向计算或主推理流程。
- 类 `Hiera`（行 1048）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1053）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_pos_embed`（行 1146）：Hiera 的内部辅助方法。
  - 方法 `forward`（行 1156）：执行前向计算或主推理流程。
- 类 `TwoWayTransformer`（行 1174）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1175）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1220）：执行前向计算或主推理流程。
- 类 `TwoWayAttentionBlock`（行 1267）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1268）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1311）：执行前向计算或主推理流程。
- 类 `Attention`（行 1345）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1351）：初始化对象状态、依赖和运行参数。
  - 方法 `_separate_heads`（行 1375）：Attention 的内部辅助方法。
  - 方法 `_recombine_heads`（行 1380）：Attention 的内部辅助方法。
  - 方法 `forward`（行 1385）：执行前向计算或主推理流程。
- 类 `RoPEAttention`（行 1412）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1415）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1434）：执行前向计算或主推理流程。
- 类 `PromptEncoder`（行 1479）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1480）：初始化对象状态、依赖和运行参数。
  - 方法 `get_dense_pe`（行 1530）：获取派生值、状态、路径或对象。
  - 方法 `_embed_points`（行 1541）：PromptEncoder 的内部辅助方法。
  - 方法 `_embed_boxes`（行 1565）：PromptEncoder 的内部辅助方法。
  - 方法 `_embed_masks`（行 1576）：PromptEncoder 的内部辅助方法。
  - 方法 `_get_batch_size`（行 1581）：PromptEncoder 的内部辅助方法。
  - 方法 `_get_device`（行 1599）：PromptEncoder 的内部辅助方法。
  - 方法 `forward`（行 1602）：执行前向计算或主推理流程。
- 类 `PositionEmbeddingSine`（行 1646）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1652）：初始化对象状态、依赖和运行参数。
  - 方法 `_encode_xy`（行 1672）：PositionEmbeddingSine 的内部辅助方法。
  - 方法 `encode_boxes`（行 1692）：把对象编码为模型或存储所需格式。
  - 方法 `encode_points`（行 1700）：把对象编码为模型或存储所需格式。
  - 方法 `forward`（行 1709）：执行前向计算或主推理流程。
- 类 `PositionEmbeddingRandom`（行 1745）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1750）：初始化对象状态、依赖和运行参数。
  - 方法 `_pe_encoding`（行 1760）：PositionEmbeddingRandom 的内部辅助方法。
  - 方法 `forward`（行 1773）：执行前向计算或主推理流程。
  - 方法 `forward_with_coords`（行 1786）：PositionEmbeddingRandom 的成员方法。
- 类 `MaskDecoder`（行 1854）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 1855）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 1949）：执行前向计算或主推理流程。
  - 方法 `predict_masks`（行 2007）：MaskDecoder 的成员方法。
  - 方法 `_get_stability_scores`（行 2090）：MaskDecoder 的内部辅助方法。
  - 方法 `_dynamic_multimask_via_stability`（行 2102）：MaskDecoder 的内部辅助方法。
- 类 `DropPath`（行 2213）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2215）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2220）：执行前向计算或主推理流程。
- 类 `MLP`（行 2233）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2234）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2252）：执行前向计算或主推理流程。
- 类 `LayerNorm2d`（行 2262）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2263）：初始化对象状态、依赖和运行参数。
  - 方法 `forward`（行 2269）：执行前向计算或主推理流程。
- 类 `SAM2Base_`（行 2276）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 2277）：初始化对象状态、依赖和运行参数。
  - 方法 `device`（行 2446）：SAM2Base_ 的成员方法。
  - 方法 `forward`（行 2449）：执行前向计算或主推理流程。
  - 方法 `_build_sam_heads`（行 2455）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_forward_sam_heads`（行 2505）：SAM2Base_ 的内部辅助方法。
  - 方法 `_use_mask_as_output`（行 2666）：SAM2Base_ 的内部辅助方法。
  - 方法 `forward_image`（行 2718）：SAM2Base_ 的成员方法。
  - 方法 `_prepare_backbone_features`（行 2732）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_prepare_memory_conditioned_features`（行 2748）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_encode_new_memory`（行 2919）：SAM2Base_ 的内部辅助方法。
  - 方法 `track_step`（行 2959）：SAM2Base_ 的成员方法。
  - 方法 `_use_multimask`（行 3056）：SAM2Base_ 的内部辅助方法。
  - 方法 `_apply_non_overlapping_constraints`（行 3066）：SAM2Base_ 的内部辅助方法。
- 类 `SAM2Base`（行 3086）：类定义，承载相关状态和行为。
  - 方法 `track_step`（行 3088）：SAM2Base 的成员方法。
  - 方法 `_forward_sam_heads`（行 3190）：SAM2Base 的内部辅助方法。
- 类 `SAM2VideoPredictor`（行 3433）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 3436）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_image_feature`（行 3454）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_single_frame_inference`（行 3488）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_consolidate_temp_output_across_obj`（行 3558）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_get_orig_video_res_output`（行 3677）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `init_state`（行 3699）：SAM2VideoPredictor 的成员方法。
  - 方法 `add_language_embd`（行 3752）：SAM2VideoPredictor 的成员方法。
  - 方法 `_clear_non_cond_mem_around_input`（行 3829）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_memory_encoder`（行 3848）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_add_output_per_object`（行 3877）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `propagate_in_video_preflight`（行 3906）：SAM2VideoPredictor 的成员方法。
  - 方法 `propagate_in_video`（行 3978）：SAM2VideoPredictor 的成员方法。
- 函数 `load_checkpoint_with_prefix`（行 29）：从文件或配置中加载数据。
- 函数 `load_state_dict_to_model`（行 66）：从文件或配置中加载数据。
- 函数 `window_partition`（行 819）：提供该文件的一段辅助逻辑。
- 函数 `window_unpartition`（行 844）：提供该文件的一段辅助逻辑。
- 函数 `do_pool`（行 900）：提供该文件的一段辅助逻辑。
- 函数 `init_t_xy`（行 1802）：提供该文件的一段辅助逻辑。
- 函数 `compute_axial_cis`（行 1809）：提供该文件的一段辅助逻辑。
- 函数 `reshape_for_broadcast`（行 1821）：提供该文件的一段辅助逻辑。
- 函数 `apply_rotary_enc`（行 1829）：提供该文件的一段辅助逻辑。
- 函数 `select_closest_cond_frames`（行 2140）：提供该文件的一段辅助逻辑。
- 函数 `get_1d_sine_pe`（行 2185）：获取派生值、状态、路径或对象。
- 函数 `get_activation_fn`（行 2198）：获取派生值、状态、路径或对象。
- 函数 `get_clones`（行 2209）：获取派生值、状态、路径或对象。
- 函数 `_obj_id_to_idx`（行 3362）：提供该文件的一段辅助逻辑。
- 函数 `_get_maskmem_pos_enc`（行 3397）：提供该文件的一段辅助逻辑。
- 函数 `_obj_idx_to_id`（行 3423）：提供该文件的一段辅助逻辑。
- 函数 `_get_obj_num`（行 3428）：提供该文件的一段辅助逻辑。
- 函数 `fill_holes_in_mask_scores`（行 4062）：提供该文件的一段辅助逻辑。
- 函数 `get_connected_components`（行 4075）：获取派生值、状态、路径或对象。

## `projects/sa2va/hf/models_qwen3vl/templates.py`

- 文件职责：HuggingFace 适配模块：提供配置、tokenizer、模型实现与下游封装。
- 行数：170
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/hooks/__init__.py`

- 文件职责：训练 Hook：在训练循环中注入同步、EMA、路由刷新等行为。
- 行数：5
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/hooks/ema_teacher_hook.py`

- 文件职责：训练 Hook：在训练循环中注入同步、EMA、路由刷新等行为。
- 行数：53
- 类 `EMATeacherHook`（行 5）：训练 Hook 类，在训练循环特定阶段注入行为。
  - 方法 `_unwrap_model`（行 11）：EMATeacherHook 的内部辅助方法。
  - 方法 `_did_optimizer_step`（行 16）：EMATeacherHook 的内部辅助方法。
  - 方法 `after_train_iter`（行 45）：EMATeacherHook 的成员方法。

## `projects/sa2va/hooks/old_policy_sync_hook.py`

- 文件职责：训练 Hook：在训练循环中注入同步、EMA、路由刷新等行为。
- 行数：20
- 类 `OldPolicySyncHook`（行 5）：训练 Hook 类，在训练循环特定阶段注入行为。
  - 方法 `_unwrap_model`（行 11）：OldPolicySyncHook 的内部辅助方法。
  - 方法 `before_train_iter`（行 15）：OldPolicySyncHook 的成员方法。

## `projects/sa2va/hooks/opsd_route_refresh_hook.py`

- 文件职责：训练 Hook：在训练循环中注入同步、EMA、路由刷新等行为。
- 行数：307
- 类 `OpsdRouteRefreshHook`（行 10）：训练 Hook 类，在训练循环特定阶段注入行为。
  - 方法 `__init__`（行 15）：初始化对象状态、依赖和运行参数。
  - 方法 `_unwrap_model`（行 34）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_dist_is_initialized`（行 39）：处理分布式训练/评测相关状态。
  - 方法 `_is_rank0`（行 42）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_barrier`（行 45）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_get_dataset_and_sampler`（行 49）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_extract_batch_sample_keys`（行 57）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_mark_batch_consumed`（行 68）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_get_global_consumed_sample_keys`（行 76）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_build_export_paths`（行 95）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_resolve_active_window_size`（行 100）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_export_routes`（行 109）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_route_state_from_route`（行 139）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_atomic_write_jsonl`（行 147）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `_resolve_checkpoint_interval`（行 155）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_build_route_state_snapshot_records`（行 170）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_save_checkpoint_route_snapshot`（行 219）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `before_train_epoch`（行 250）：OpsdRouteRefreshHook 的成员方法。
  - 方法 `_refresh_dataset_and_log`（行 261）：OpsdRouteRefreshHook 的内部辅助方法。
  - 方法 `after_train_iter`（行 277）：OpsdRouteRefreshHook 的成员方法。

## `projects/sa2va/models/__init__.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：36
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/models/extension/__init__.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：1
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/models/extension/sam2_base.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：281
- 类 `SAM2Base`（行 8）：类定义，承载相关状态和行为。
  - 方法 `track_step`（行 10）：SAM2Base 的成员方法。
  - 方法 `_forward_sam_heads`（行 112）：SAM2Base 的内部辅助方法。

## `projects/sa2va/models/mllm/__init__.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：1
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/models/mllm/internvl.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：315
- 类 `InternVLMLLM`（行 12）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 13）：初始化对象状态、依赖和运行参数。
  - 方法 `add_special_tokens`（行 58）：InternVLMLLM 的成员方法。
  - 方法 `manual_prepare_llm_for_lora`（行 69）：InternVLMLLM 的成员方法。
  - 方法 `get_embedding_size`（行 73）：获取派生值、状态、路径或对象。
  - 方法 `state_dict`（行 77）：InternVLMLLM 的成员方法。
  - 方法 `forward`（行 80）：执行前向计算或主推理流程。
  - 方法 `_llm_forward`（行 135）：InternVLMLLM 的内部辅助方法。
  - 方法 `_process_visual_prompts`（行 215）：InternVLMLLM 的内部辅助方法。
  - 方法 `_embed_visual_features`（行 269）：InternVLMLLM 的内部辅助方法。
  - 方法 `_compute_loss`（行 303）：计算内部指标、几何关系或辅助数值。

## `projects/sa2va/models/mllm/qwen3vl.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：284
- 类 `Qwen3VL`（行 19）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 26）：初始化对象状态、依赖和运行参数。
  - 方法 `add_special_tokens`（行 84）：Qwen3VL 的成员方法。
  - 方法 `_init_processor`（行 94）：初始化内部子模块、配置或运行时状态。
  - 方法 `_parse_lora_config`（行 101）：Qwen3VL 的内部辅助方法。
  - 方法 `_prepare_llm_for_lora`（行 107）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `manual_prepare_llm_for_lora`（行 123）：Qwen3VL 的成员方法。
  - 方法 `get_embedding_size`（行 128）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 132）：执行前向计算或主推理流程。
  - 方法 `generate`（行 156）：Qwen3VL 的成员方法。
  - 方法 `state_dict`（行 175）：Qwen3VL 的成员方法。
  - 方法 `init_weights`（行 185）：Qwen3VL 的成员方法。

## `projects/sa2va/models/mllm/qwenvl.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：393
- 类 `Qwen2_5_VL`（行 18）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 23）：初始化对象状态、依赖和运行参数。
  - 方法 `add_special_tokens`（行 70）：Qwen2_5_VL 的成员方法。
  - 方法 `_init_processor`（行 80）：初始化内部子模块、配置或运行时状态。
  - 方法 `_parse_lora_config`（行 87）：Qwen2_5_VL 的内部辅助方法。
  - 方法 `_prepare_llm_for_lora`（行 93）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `manual_prepare_llm_for_lora`（行 109）：Qwen2_5_VL 的成员方法。
  - 方法 `get_embedding_size`（行 114）：获取派生值、状态、路径或对象。
  - 方法 `_normalize_prompt_masks`（行 118）：Qwen2_5_VL 的内部辅助方法。
  - 方法 `_resolve_prompt_masks_per_image`（行 129）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_resize_prompt_masks_for_grid`（行 190）：Qwen2_5_VL 的内部辅助方法。
  - 方法 `_build_inputs_embeds_with_visual_prompts`（行 203）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `forward`（行 268）：执行前向计算或主推理流程。
  - 方法 `state_dict`（行 311）：Qwen2_5_VL 的成员方法。
  - 方法 `init_weights`（行 321）：Qwen2_5_VL 的成员方法。

## `projects/sa2va/models/predictor/__init__.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：1
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/models/predictor/sam2_predictor.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：708
- 类 `SAM2VideoPredictor`（行 82）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 85）：初始化对象状态、依赖和运行参数。
  - 方法 `_get_image_feature`（行 103）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_single_frame_inference`（行 136）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_consolidate_temp_output_across_obj`（行 206）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_get_orig_video_res_output`（行 325）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `init_state`（行 347）：SAM2VideoPredictor 的成员方法。
  - 方法 `add_language_embd`（行 400）：SAM2VideoPredictor 的成员方法。
  - 方法 `_clear_non_cond_mem_around_input`（行 477）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_run_memory_encoder`（行 496）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `_add_output_per_object`（行 525）：SAM2VideoPredictor 的内部辅助方法。
  - 方法 `propagate_in_video_preflight`（行 554）：SAM2VideoPredictor 的成员方法。
  - 方法 `propagate_in_video`（行 626）：SAM2VideoPredictor 的成员方法。
- 函数 `_obj_id_to_idx`（行 11）：提供该文件的一段辅助逻辑。
- 函数 `_get_maskmem_pos_enc`（行 46）：提供该文件的一段辅助逻辑。
- 函数 `_obj_idx_to_id`（行 72）：提供该文件的一段辅助逻辑。
- 函数 `_get_obj_num`（行 77）：提供该文件的一段辅助逻辑。

## `projects/sa2va/models/preprocess/__init__.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：3
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/models/preprocess/image_resize.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：14
- 类 `DirectResize`（行 5）：类定义，承载相关状态和行为。
  - 方法 `__init__`（行 6）：初始化对象状态、依赖和运行参数。
  - 方法 `apply_image`（行 9）：DirectResize 的成员方法。

## `projects/sa2va/models/sa2va.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：366
- 类 `Sa2VAModel`（行 20）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 21）：初始化对象状态、依赖和运行参数。
  - 方法 `_add_special_tokens`（行 142）：Sa2VAModel 的内部辅助方法。
  - 方法 `load_state_dict`（行 146）：从文件或配置中加载数据。
  - 方法 `_merge_lora`（行 149）：合并多个中间结果或统计项。
  - 方法 `all_state_dict`（行 164）：Sa2VAModel 的成员方法。
  - 方法 `state_dict`（行 168）：Sa2VAModel 的成员方法。
  - 方法 `check_obj_number`（行 184）：Sa2VAModel 的成员方法。
  - 方法 `_get_pesudo_data`（行 208）：Sa2VAModel 的内部辅助方法。
  - 方法 `forward`（行 216）：执行前向计算或主推理流程。
  - 方法 `sample_points`（行 319）：Sa2VAModel 的成员方法。
  - 方法 `generate_video_pred_embeddings`（行 333）：Sa2VAModel 的成员方法。
  - 方法 `process_video_gt_masks`（行 340）：Sa2VAModel 的成员方法。
  - 方法 `preparing_for_generation`（行 353）：Sa2VAModel 的成员方法。
- 函数 `get_seg_hidden_states`（行 356）：获取派生值、状态、路径或对象。
- 函数 `mask_to_rle`（行 361）：把二值 mask 编码为 RLE。

## `projects/sa2va/models/sa2va_opsd.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：898
- 类 `DescriptionResult`（行 20）：类定义，承载相关状态和行为。
  - 方法：无显式方法定义。
- 类 `ReconstructionResult`（行 28）：类定义，承载相关状态和行为。
  - 方法：无显式方法定义。
- 类 `Sa2VAOPSDModel`（行 36）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 39）：初始化对象状态、依赖和运行参数。
  - 方法 `_validate_device`（行 127）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_resolve_torch_dtype`（行 139）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_load_model`（行 148）：加载内部所需的资源、配置或标注数据。
  - 方法 `_ensure_runtime_state`（行 162）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_ensure_generation_ready`（行 166）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `has_teacher_model`（行 173）：Sa2VAOPSDModel 的成员方法。
  - 方法 `require_teacher_model`（行 176）：Sa2VAOPSDModel 的成员方法。
  - 方法 `_apply_generation_overrides`（行 185）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `train`（行 191）：Sa2VAOPSDModel 的成员方法。
  - 方法 `to`（行 197）：Sa2VAOPSDModel 的成员方法。
  - 方法 `_sync_teacher`（行 211）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `update_teacher_ema`（行 220）：Sa2VAOPSDModel 的成员方法。
  - 方法 `state_dict`（行 250）：Sa2VAOPSDModel 的成员方法。
  - 方法 `load_state_dict`（行 255）：从文件或配置中加载数据。
  - 方法 `_to_numpy_mask`（行 265）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_strip_image_placeholder`（行 271）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_normalize_student_question`（行 274）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_create_region_prompt`（行 277）：创建新的提示、张量、字符串或数据记录。
  - 方法 `_format_mask_prompts_for_predict_forward`（行 299）：整理输出格式或转换为约定的数据表示。
  - 方法 `_build_forward_inputs`（行 306）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_build_forward_inputs_no_vp`（行 376）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_extract_vit_embeds`（行 439）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_compose_inputs_embeds`（行 442）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_generate_with_model`（行 488）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_extract_completion_ids`（行 501）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_clean_caption_text`（行 513）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_infer_description_status`（行 528）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_encode_completion_from_caption`（行 545）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `_mask_bbox`（行 554）：围绕 mask 的几何属性、可视化或统计提供辅助逻辑。
  - 方法 `_should_debug_print`（行 563）：输出调试信息或构造调试样本。
  - 方法 `_debug_sample`（行 570）：输出调试信息或构造调试样本。
  - 方法 `_predict_forward_eval`（行 609）：封装一次预测或推理调用。
  - 方法 `_forward_sequence_with_model`（行 619）：Sa2VAOPSDModel 的内部辅助方法。
  - 方法 `generate_description`（行 635）：Sa2VAOPSDModel 的成员方法。
  - 方法 `reconstruct_mask_from_description`（行 656）：Sa2VAOPSDModel 的成员方法。
  - 方法 `_compute_iou`（行 704）：计算内部指标、几何关系或辅助数值。
  - 方法 `build_teacher_privileged_prompt`（行 719）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `generalized_jsd_loss`（行 731）：Sa2VAOPSDModel 的成员方法。
  - 方法 `compute_opsd_loss`（行 758）：Sa2VAOPSDModel 的成员方法。
  - 方法 `forward`（行 782）：执行前向计算或主推理流程。
- 函数 `get_seg_hidden_states`（行 11）：获取派生值、状态、路径或对象。

## `projects/sa2va/models/sa2va_opsd_v2.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：3972
- 类 `DescriptionResult`（行 43）：类定义，承载相关状态和行为。
  - 方法：无显式方法定义。
- 类 `ReconstructionResult`（行 51）：类定义，承载相关状态和行为。
  - 方法：无显式方法定义。
- 类 `ConfuserSelectionResult`（行 61）：类定义，承载相关状态和行为。
  - 方法：无显式方法定义。
- 类 `Sa2VAOPSDModelV2`（行 70）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `__init__`（行 73）：初始化对象状态、依赖和运行参数。
  - 方法 `_validate_device`（行 328）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_teacher_routes_require_teacher_model`（行 339）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_resolve_torch_dtype`（行 349）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_load_model`（行 358）：加载内部所需的资源、配置或标注数据。
  - 方法 `_prefer_non_reentrant_gradient_checkpointing`（行 409）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_patch_legacy_gradient_checkpointing_modules`（行 430）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_resolve_non_reentrant_checkpoint_fn`（行 436）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_patch_intern_vision_encoder_forward`（行 447）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_gradient_checkpointing_candidates`（行 481）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_ensure_runtime_state`（行 494）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_ensure_generation_ready`（行 498）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `has_teacher_model`（行 509）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `has_old_policy_model`（行 512）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `require_teacher_model`（行 515）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `require_old_policy_model`（行 523）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `train`（行 529）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `to`（行 537）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `_sync_old_policy`（行 556）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_sync_teacher`（行 565）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `update_teacher_ema`（行 574）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `_metric_tensor`（行 604）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `state_dict`（行 607）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `load_state_dict`（行 610）：从文件或配置中加载数据。
  - 方法 `_to_numpy_mask`（行 644）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_normalize_prompt_masks_array`（行 650）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_build_mask_focused_image`（行 664）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_clean_caption_text`（行 686）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_infer_description_status`（行 714）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_caption_token_count`（行 788）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_is_caption_content_sufficient`（行 791）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_caption_scene_spill_hit_count`（行 823）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_is_low_density_long_caption`（行 868）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_description_quality_score`（行 880）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_caption_quality_reward`（行 897）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_format_float_list`（行 928）：整理输出格式或转换为约定的数据表示。
  - 方法 `_has_repetitive_caption_pattern`（行 932）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_is_np_like_caption`（行 962）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_is_overly_generic_caption`（行 969）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_resolve_reconstruct_questions`（行 1062）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_teacher_regenerate_iou_improvement`（行 1067）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_teacher_regenerate_gate_passed`（行 1071）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_grpo_reward_from_iou`（行 1074）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_invalid_reconstruction_placeholder`（行 1085）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_canonicalize_referring_expression`（行 1096）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_subject_only_referring_expression`（行 1124）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_append_spatial_hint_to_question`（行 1135）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_coarse_spatial_hint`（行 1145）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_encode_completion_from_caption`（行 1167）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_mask_bbox`（行 1176）：围绕 mask 的几何属性、可视化或统计提供辅助逻辑。
  - 方法 `_resolve_grpo_option_token_ids`（行 1185）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_should_debug_print`（行 1197）：输出调试信息或构造调试样本。
  - 方法 `_dist_is_initialized`（行 1205）：处理分布式训练/评测相关状态。
  - 方法 `_dist_rank`（行 1209）：处理分布式训练/评测相关状态。
  - 方法 `_log_ddp_route_alignment_debug`（行 1214）：输出调试信息或构造调试样本。
  - 方法 `_log_pre_return_debug`（行 1342）：输出调试信息或构造调试样本。
  - 方法 `_debug_sample`（行 1394）：输出调试信息或构造调试样本。
  - 方法 `_temporary_eval_model`（行 1449）：提供临时上下文或状态切换。
  - 方法 `_predict_forward_eval`（行 1457）：封装一次预测或推理调用。
  - 方法 `predict_text_with_masks`（行 1465）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `_format_mask_prompts_for_predict_forward`（行 1490）：整理输出格式或转换为约定的数据表示。
  - 方法 `_to_teacher_prompt_masks`（行 1501）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_strip_image_placeholder`（行 1510）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_normalize_student_question`（行 1513）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_create_region_prompt`（行 1516）：创建新的提示、张量、字符串或数据记录。
  - 方法 `_build_forward_inputs`（行 1538）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_extract_vit_embeds`（行 1600）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_compose_inputs_embeds`（行 1603）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_forward_sequence_with_model`（行 1642）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_pad_tensor_rows`（行 1682）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_pad_sequence_batch`（行 1705）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_build_full_sequence_batch`（行 1730）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_forward_sequence_multi_sample_with_model`（行 1814）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_masked_token_mean`（行 1858）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_sequence_cross_entropy_batch_from_logits`（行 1863）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_forward_sequence_batch_with_model`（行 1871）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `predict_teacher_caption_on_student_trajectory`（行 1924）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `generate_description_with_model`（行 1966）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `generate_description`（行 1997）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `generate_teacher_caption_with_privileged_prompt`（行 2006）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `reconstruct_mask`（行 2047）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `_compute_iou`（行 2097）：计算内部指标、几何关系或辅助数值。
  - 方法 `_prepare_pred_mask_for_iou`（行 2112）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_mask_summary`（行 2125）：围绕 mask 的几何属性、可视化或统计提供辅助逻辑。
  - 方法 `build_teacher_privileged_prompt_v3`（行 2139）：构建对外可用的数据结构、模型、提示词或运行对象。
  - 方法 `generalized_jsd_token_loss`（行 2237）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `_sequence_cross_entropy_from_logits`（行 2263）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_token_log_probs_from_logits`（行 2268）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_materialize_autograd_input`（行 2274）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_route_from_iou`（行 2281）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_build_training_teacher_fields`（行 2289）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_route_prompt_tag`（行 2296）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `estimate_opsd_route_for_sample_with_model`（行 2307）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `estimate_opsd_route_for_sample`（行 2440）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `_resolve_batch_route`（行 2462）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_resolve_loss_family`（行 2471）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_build_dummy_completion_ids`（行 2495）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_build_teacher_prompt_masks`（行 2506）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_zero_ref_mask_like`（行 2516）：创建零值/空值占位结果。
  - 方法 `_merge_dummy_reasons`（行 2520）：合并多个中间结果或统计项。
  - 方法 `_attempt_teacher_regenerate_analysis`（行 2528）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `_empty_loss_vector`（行 2598）：创建零值/空值占位结果。
  - 方法 `_zero_scalar`（行 2601）：创建零值/空值占位结果。
  - 方法 `_placeholder_loss_vector`（行 2606）：Sa2VAOPSDModelV2 的内部辅助方法。
  - 方法 `compute_regenerate_alignment_loss`（行 2618）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `compute_regenerate_alignment_losses_batch`（行 2639）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `compute_onpolicy_distill_loss`（行 2665）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `compute_onpolicy_distill_losses_batch`（行 2707）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `_sample_grpo_descriptions`（行 2736）：执行采样逻辑，生成训练或评估所需候选。
  - 方法 `_mask_area_ratio`（行 2754）：围绕 mask 的几何属性、可视化或统计提供辅助逻辑。
  - 方法 `_mask_center`（行 2761）：围绕 mask 的几何属性、可视化或统计提供辅助逻辑。
  - 方法 `_mask_center_distance`（行 2770）：围绕 mask 的几何属性、可视化或统计提供辅助逻辑。
  - 方法 `_prepare_mask_like_gt`（行 2777）：预处理内部输入，转换为后续步骤可直接消费的形式。
  - 方法 `_score_confuser_candidate`（行 2786）：对候选项、样本或轨迹进行打分。
  - 方法 `_select_confuser_masks`（行 2794）：从候选集合中筛选符合条件的项。
  - 方法 `_build_confuser_mcq_prompt`（行 2846）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_score_caption_against_mask_options`（行 2856）：对候选项、样本或轨迹进行打分。
  - 方法 `compute_grpo_loss`（行 2905）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `compute_grpo_losses_batch`（行 3124）：Sa2VAOPSDModelV2 的成员方法。
  - 方法 `forward`（行 3254）：执行前向计算或主推理流程。

## `projects/sa2va/models/sa2va_opsd_v3.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：84
- 类 `Sa2VAOPSDModelV3`（行 8）：模型类，封装网络结构、前向流程与训练逻辑。
  - 方法 `_resolve_runtime_device`（行 12）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `__init__`（行 24）：初始化对象状态、依赖和运行参数。
  - 方法 `_disable_gradient_checkpointing_for_ddp`（行 41）：Sa2VAOPSDModelV3 的内部辅助方法。
  - 方法 `_ddp_is_initialized`（行 60）：Sa2VAOPSDModelV3 的内部辅助方法。
  - 方法 `_ddp_route_safety_loss`（行 63）：Sa2VAOPSDModelV3 的内部辅助方法。
  - 方法 `forward`（行 74）：执行前向计算或主推理流程。

## `projects/sa2va/models/sam2_train.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：152
- 类 `SAM2TrainRunner`（行 17）：运行器/封装类，组织训练或推理过程。
  - 方法 `__init__`（行 18）：初始化对象状态、依赖和运行参数。
  - 方法 `preprocess_image`（行 63）：SAM2TrainRunner 的成员方法。
  - 方法 `inject_language_embd`（行 71）：SAM2TrainRunner 的成员方法。
  - 方法 `get_sam2_embeddings`（行 105）：获取派生值、状态、路径或对象。
  - 方法 `get_sam2_feats`（行 128）：获取派生值、状态、路径或对象。
  - 方法 `get_sam2_states_from_feats`（行 134）：获取派生值、状态、路径或对象。
  - 方法 `forward`（行 151）：执行前向计算或主推理流程。

## `projects/sa2va/models/utils.py`

- 文件职责：核心模型模块：定义 Sa2VA/OPSD 模型、辅助组件与训练封装。
- 行数：83
- 函数 `find_closest_aspect_ratio`（行 2）：搜索最匹配的对象、索引或路径。
- 函数 `dynamic_preprocess`（行 18）：提供该文件的一段辅助逻辑。
- 函数 `find_seg_indices`（行 63）：搜索最匹配的对象、索引或路径。

## `projects/sa2va/samplers/__init__.py`

- 文件职责：采样器模块：控制样本分组、分布式切分与消费状态。
- 行数：3
- 顶层符号：无函数/类定义，主要由常量、配置、导出语句或脚本参数组成。

## `projects/sa2va/samplers/route_grouped_sampler.py`

- 文件职责：采样器模块：控制样本分组、分布式切分与消费状态。
- 行数：188
- 类 `RouteGroupedSampler`（行 9）：采样器类，控制迭代顺序、分组和分布式切分。
  - 方法 `__init__`（行 12）：初始化对象状态、依赖和运行参数。
  - 方法 `_dist_is_initialized`（行 48）：处理分布式训练/评测相关状态。
  - 方法 `_resolve_rank`（行 51）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `_resolve_world_size`（行 58）：解析并确定运行时所需的路径、配置、状态或目标对象。
  - 方法 `global_batch_size`（行 66）：RouteGroupedSampler 的成员方法。
  - 方法 `set_epoch`（行 69）：设置运行状态、属性或随机种子。
  - 方法 `_get_sample_key`（行 72）：RouteGroupedSampler 的内部辅助方法。
  - 方法 `_get_route`（行 83）：RouteGroupedSampler 的内部辅助方法。
  - 方法 `_build_route_buckets`（行 95）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_build_rank_indices`（行 112）：构造中间对象、提示词、样本结构或运行配置。
  - 方法 `_get_manifest_version`（行 140）：RouteGroupedSampler 的内部辅助方法。
  - 方法 `_refresh_cached_indices_if_needed`（行 146）：RouteGroupedSampler 的内部辅助方法。
  - 方法 `mark_consumed_sample_keys`（行 157）：RouteGroupedSampler 的成员方法。
  - 方法 `replace_consumed_sample_keys`（行 162）：RouteGroupedSampler 的成员方法。
  - 方法 `reset_consumed_sample_keys`（行 165）：RouteGroupedSampler 的成员方法。
  - 方法 `get_consumed_sample_keys`（行 168）：获取派生值、状态、路径或对象。
  - 方法 `get_consumed_sample_count`（行 171）：获取派生值、状态、路径或对象。
  - 方法 `__iter__`（行 174）：RouteGroupedSampler 的内部辅助方法。
  - 方法 `__len__`（行 186）：返回数据集或容器长度。

## `tools/conf.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：300
- Shell 函数：
  - `usage`（行 23）：输出命令行脚本的使用说明。
  - `validate_positive_int`（行 44）：检查输入、环境或参数是否合法。
  - `validate_nonnegative_int`（行 54）：检查输入、环境或参数是否合法。
  - `realign_venv_from_activate`（行 64）：提供该文件的一段辅助逻辑。
  - `resolve_python_bin`（行 84）：提供该文件的一段辅助逻辑。
  - `validate_environment`（行 113）：检查输入、环境或参数是否合法。

## `tools/convert_to_hf.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：197
- 函数 `convert_dict2config_dict`（行 14）：提供该文件的一段辅助逻辑。
- 函数 `parse_args`（行 24）：解析命令行参数。
- 函数 `master_print`（行 34）：提供该文件的一段辅助逻辑。
- 函数 `main`（行 37）：脚本主入口，组织完整执行流程。

## `tools/convert_to_pth.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：89
- 函数 `parse_args`（行 9）：解析命令行参数。
- 函数 `master_print`（行 21）：提供该文件的一段辅助逻辑。
- 函数 `main`（行 24）：脚本主入口，组织完整执行流程。

## `tools/debug_opsd_first100.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：155
- 函数 `parse_args`（行 12）：解析命令行参数。
- 函数 `main`（行 26）：脚本主入口，组织完整执行流程。

## `tools/diagnose_opsd_sample.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：243
- 函数 `normalize_frame`（行 12）：把原始图像帧归一化为可视化/推理格式。
- 函数 `normalize_mask`（行 28）：把原始 mask 归一化为标准二值格式。
- 函数 `clean_caption_text`（行 37）：提供该文件的一段辅助逻辑。
- 函数 `coarse_spatial_hint`（行 50）：提供该文件的一段辅助逻辑。
- 函数 `compute_iou`（行 72）：计算两个 mask 的 IoU。
- 函数 `canonicalize_caption`（行 86）：提供该文件的一段辅助逻辑。
- 函数 `compress_caption`（行 98）：提供该文件的一段辅助逻辑。
- 函数 `first_mask`（行 117）：提供该文件的一段辅助逻辑。
- 函数 `main`（行 129）：脚本主入口，组织完整执行流程。

## `tools/dist.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：51
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/eval/eval_davis.py`

- 文件职责：评测工具脚本：针对具体 benchmark 调度结果读取与指标评估。
- 行数：126
- 函数 `eval_queue`（行 17）：提供该文件的一段辅助逻辑。
- 函数 `get_meta_exp`（行 49）：获取派生值、状态、路径或对象。

## `tools/eval/eval_mevis.py`

- 文件职责：评测工具脚本：针对具体 benchmark 调度结果读取与指标评估。
- 行数：107
- 函数 `eval_queue`（行 21）：提供该文件的一段辅助逻辑。

## `tools/eval/eval_ref_sav.py`

- 文件职责：评测工具脚本：针对具体 benchmark 调度结果读取与指标评估。
- 行数：174
- 函数 `eval_queue`（行 28）：提供该文件的一段辅助逻辑。

## `tools/eval/eval_revos.py`

- 文件职责：评测工具脚本：针对具体 benchmark 调度结果读取与指标评估。
- 行数：184
- 函数 `eval_queue`（行 22）：提供该文件的一段辅助逻辑。

## `tools/eval_refcoco_caption_to_mask.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：150
- 函数 `parse_args`（行 22）：解析命令行参数。
- 函数 `load_config`（行 32）：加载并解析配置文件。
- 函数 `resolve_image_root`（行 36）：提供该文件的一段辅助逻辑。
- 函数 `decode_ref_mask`（行 50）：把编码后的对象还原为可操作的数据结构。
- 函数 `build_samples`（行 66）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `main`（行 97）：脚本主入口，组织完整执行流程。

## `tools/eval_refcoco_mask_closure.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：394
- 函数 `parse_args`（行 24）：解析命令行参数。
- 函数 `load_config`（行 35）：加载并解析配置文件。
- 函数 `resolve_image_root`（行 39）：提供该文件的一段辅助逻辑。
- 函数 `mask_to_rle`（行 53）：把二值 mask 编码为 RLE。
- 函数 `parse_teacher_diagnosis_output`（行 59）：提供该文件的一段辅助逻辑。
- 函数 `normalize_teacher_output`（行 82）：规范化输入数据、路径或文本表示。
- 函数 `teacher_parsed_field_count`（行 88）：提供该文件的一段辅助逻辑。
- 函数 `is_degenerate_teacher_output`（行 98）：提供该文件的一段辅助逻辑。
- 函数 `score_teacher_output`（行 118）：提供该文件的一段辅助逻辑。
- 函数 `build_teacher_diagnosis_prompt_variants`（行 129）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `build_teacher_diagnosis_fallback`（行 191）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `format_teacher_structured_output`（行 230）：提供该文件的一段辅助逻辑。
- 函数 `build_teacher_diagnosis_prompt`（行 240）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `build_teacher_diagnosis_fields`（行 244）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `main`（行 254）：脚本主入口，组织完整执行流程。

## `tools/eval_refcoco_mask_closure_official.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：93
- 函数 `parse_args`（行 14）：解析命令行参数。
- 函数 `load_config`（行 26）：加载并解析配置文件。
- 函数 `resolve_image_root`（行 30）：提供该文件的一段辅助逻辑。
- 函数 `main`（行 44）：脚本主入口，组织完整执行流程。

## `tools/eval_teacher_privileged_diagnosis.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：312
- 函数 `parse_args`（行 25）：解析命令行参数。
- 函数 `mask_stats`（行 37）：提供该文件的一段辅助逻辑。
- 函数 `build_teacher_diagnosis_prompt`（行 58）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `summarize_results`（行 122）：汇总结果、统计项或评估摘要。
- 函数 `main`（行 208）：脚本主入口，组织完整执行流程。

## `tools/eval_teacher_privileged_diagnosis_refcoco.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：392
- 函数 `parse_args`（行 27）：解析命令行参数。
- 函数 `load_config`（行 37）：加载并解析配置文件。
- 函数 `init_distributed_if_needed`（行 41）：提供该文件的一段辅助逻辑。
- 函数 `finalize_distributed`（行 52）：提供该文件的一段辅助逻辑。
- 函数 `resolve_runtime_device`（行 57）：提供该文件的一段辅助逻辑。
- 函数 `build_rank_output_path`（行 68）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `summarize_results`（行 74）：汇总结果、统计项或评估摘要。
- 函数 `mask_stats`（行 162）：提供该文件的一段辅助逻辑。
- 函数 `build_teacher_diagnosis_prompt`（行 183）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `main`（行 246）：脚本主入口，组织完整执行流程。

## `tools/export.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：79
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/export_opsd_mask_to_caption_refcoco_style.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：173
- 函数 `parse_args`（行 24）：解析命令行参数。
- 函数 `mask_to_rle`（行 40）：把二值 mask 编码为 RLE。
- 函数 `save_image`（行 47）：把结果写回磁盘或序列化输出。
- 函数 `main`（行 57）：脚本主入口，组织完整执行流程。

## `tools/export_opsd_routes.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：822
- 函数 `parse_args`（行 27）：解析命令行参数。
- 函数 `_merge_cfg_options`（行 59）：合并多个中间结果或统计项。
- 函数 `load_config`（行 73）：加载并解析配置文件。
- 函数 `load_checkpoint_if_needed`（行 78）：按需加载 checkpoint。
- 函数 `dist_is_initialized`（行 87）：提供该文件的一段辅助逻辑。
- 函数 `get_rank`（行 91）：获取派生值、状态、路径或对象。
- 函数 `get_world_size`（行 97）：获取派生值、状态、路径或对象。
- 函数 `is_rank0`（行 103）：提供该文件的一段辅助逻辑。
- 函数 `maybe_init_distributed`（行 107）：提供该文件的一段辅助逻辑。
- 函数 `sync_success_or_raise`（行 116）：提供该文件的一段辅助逻辑。
- 函数 `barrier`（行 127）：提供该文件的一段辅助逻辑。
- 函数 `get_coord_dir`（行 132）：获取派生值、状态、路径或对象。
- 函数 `get_coord_timeout_seconds`（行 137）：获取派生值、状态、路径或对象。
- 函数 `get_coord_poll_seconds`（行 141）：获取派生值、状态、路径或对象。
- 函数 `atomic_write_text`（行 145）：提供该文件的一段辅助逻辑。
- 函数 `atomic_write_json`（行 153）：提供该文件的一段辅助逻辑。
- 函数 `load_json`（行 157）：从文件或配置中加载数据。
- 函数 `load_jsonl_records`（行 162）：从文件或配置中加载数据。
- 函数 `load_manifest_sample_keys`（行 173）：从文件或配置中加载数据。
- 函数 `write_jsonl_records`（行 182）：把结果写回磁盘或序列化输出。
- 函数 `wait_for_paths`（行 189）：提供该文件的一段辅助逻辑。
- 函数 `get_timestamp_path`（行 200）：获取派生值、状态、路径或对象。
- 函数 `get_result_path`（行 204）：获取派生值、状态、路径或对象。
- 函数 `build_rank_status_path`（行 208）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `build_model_from_cfg`（行 213）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `collect_refcoco_samples_from_cfg`（行 224）：提供该文件的一段辅助逻辑。
- 函数 `build_confuser_dataset_from_cfg`（行 249）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `filter_samples_by_sample_keys`（行 264）：依据条件过滤数据或记录。
- 函数 `shard_samples`（行 274）：提供该文件的一段辅助逻辑。
- 函数 `build_manifest_record`（行 280）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `build_skip_manifest_record`（行 292）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `build_rank_shard_path`（行 304）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `iter_batches`（行 308）：提供该文件的一段辅助逻辑。
- 函数 `export_routes_shard`（行 315）：导出中间结果、路由、模型或数据产物。
- 函数 `merge_route_counts`（行 394）：提供该文件的一段辅助逻辑。
- 函数 `merge_counter_dicts`（行 402）：提供该文件的一段辅助逻辑。
- 函数 `summarize_route_counts`（行 410）：汇总结果、统计项或评估摘要。
- 函数 `count_active_records`（行 420）：提供该文件的一段辅助逻辑。
- 函数 `merge_manifest_records`（行 428）：提供该文件的一段辅助逻辑。
- 函数 `merge_shards`（行 445）：提供该文件的一段辅助逻辑。
- 函数 `cleanup_shards`（行 457）：清理临时资源或退出状态。
- 函数 `export_routes`（行 463）：导出中间结果、路由、模型或数据产物。
- 函数 `export_routes_from_runner`（行 707）：导出中间结果、路由、模型或数据产物。
- 函数 `main`（行 767）：脚本主入口，组织完整执行流程。

## `tools/export_refcoco_opsd_routes_2b.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：14
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/export_refcoco_opsd_routes_4b.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：14
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/export_refcoco_opsd_routes_impl.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：504
- Shell 函数：
  - `count_csv_items`（行 40）：提供该文件的一段辅助逻辑。
  - `build_cuda_device_ids`（行 61）：构建对外可用的数据结构、模型、提示词或运行对象。
  - `validate_gpu_count`（行 76）：检查输入、环境或参数是否合法。
  - `validate_positive_int`（行 85）：检查输入、环境或参数是否合法。
  - `validate_cuda_device_ids`（行 95）：检查输入、环境或参数是否合法。
  - `realign_venv_from_activate`（行 123）：提供该文件的一段辅助逻辑。
  - `resolve_python_bin`（行 143）：提供该文件的一段辅助逻辑。
  - `validate_export_environment`（行 173）：检查输入、环境或参数是否合法。
  - `usage`（行 205）：输出命令行脚本的使用说明。

## `tools/export_refcoco_sam_confuser_pool.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：441
- 函数 `parse_args`（行 28）：解析命令行参数。
- 函数 `normalize_refcoco_data_root`（行 66）：规范化输入数据、路径或文本表示。
- 函数 `normalize_sam2_config_name`（行 73）：规范化输入数据、路径或文本表示。
- 函数 `decode_coco_rle`（行 82）：把编码后的对象还原为可操作的数据结构。
- 函数 `sanitize_coco_rle`（行 93）：提供该文件的一段辅助逻辑。
- 函数 `compute_iou`（行 101）：计算两个 mask 的 IoU。
- 函数 `group_records_by_image`（行 111）：按照键或规则对样本进行分组。
- 函数 `build_generator`（行 129）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `prepare_output_path`（行 150）：提供该文件的一段辅助逻辑。
- 函数 `atomic_write_gzip_json`（行 154）：提供该文件的一段辅助逻辑。
- 函数 `load_image`（行 162）：从文件或配置中加载数据。
- 函数 `filter_mask_records`（行 169）：依据条件过滤数据或记录。
- 函数 `process_image_record`（行 242）：提供该文件的一段辅助逻辑。
- 函数 `validate_args`（行 301）：检查输入、环境或参数是否合法。
- 函数 `main`（行 314）：脚本主入口，组织完整执行流程。

## `tools/plot_training_metrics.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：237
- 函数 `parse_args`（行 22）：解析命令行参数。
- 函数 `find_candidate_files`（行 57）：搜索最匹配的对象、索引或路径。
- 函数 `parse_json_lines`（行 79）：提供该文件的一段辅助逻辑。
- 函数 `parse_plain_json`（行 104）：提供该文件的一段辅助逻辑。
- 函数 `parse_log_text`（行 126）：提供该文件的一段辅助逻辑。
- 函数 `merge_metric_data`（行 148）：提供该文件的一段辅助逻辑。
- 函数 `moving_average`（行 159）：提供该文件的一段辅助逻辑。
- 函数 `choose_parser`（行 174）：提供该文件的一段辅助逻辑。
- 函数 `plot_metrics`（行 186）：绘制图表或可视化结果。
- 函数 `main`（行 214）：脚本主入口，组织完整执行流程。

## `tools/run_teacher_context_validation_refcoco_2b_4gpu.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：37
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/run_teacher_context_validation_refcoco_4b_4gpu.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：37
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/slurm.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：31
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/test.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：125
- 函数 `parse_args`（行 19）：解析命令行参数。
- 函数 `register_function`（行 53）：提供该文件的一段辅助逻辑。
- 函数 `main`（行 68）：脚本主入口，组织完整执行流程。

## `tools/testconf.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：184
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/train.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：65
- 函数 `parse_args`（行 6）：解析命令行参数。
- 函数 `disable_torch_repr`（行 44）：提供该文件的一段辅助逻辑。
- 函数 `enable_torch_repr`（行 50）：提供该文件的一段辅助逻辑。
- 函数 `enable_short_torch_repr`（行 57）：提供该文件的一段辅助逻辑。

## `tools/train.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：116
- Shell 函数：
  - `cleanup`（行 86）：清理临时资源或退出状态。

## `tools/train1.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：98
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/train_refcoco_opsd.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：8
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/train_refcoco_opsd_2b.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：15
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/train_refcoco_opsd_4b.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：15
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/train_refcoco_opsd_impl.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：582
- Shell 函数：
  - `count_csv_items`（行 41）：提供该文件的一段辅助逻辑。
  - `build_cuda_device_ids`（行 62）：构建对外可用的数据结构、模型、提示词或运行对象。
  - `validate_gpu_count`（行 77）：检查输入、环境或参数是否合法。
  - `validate_positive_int`（行 86）：检查输入、环境或参数是否合法。
  - `resolve_checkpoint_step`（行 96）：提供该文件的一段辅助逻辑。
  - `validate_cuda_device_ids`（行 119）：检查输入、环境或参数是否合法。
  - `realign_venv_from_activate`（行 147）：提供该文件的一段辅助逻辑。
  - `resolve_python_bin`（行 167）：提供该文件的一段辅助逻辑。
  - `validate_flash_attn_installation`（行 197）：检查输入、环境或参数是否合法。
  - `usage`（行 219）：输出命令行脚本的使用说明。

## `tools/train_resume.sh`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：93
- Shell 函数：无显式 shell function，文件主要由环境变量设置和命令调用组成。

## `tools/visualize_opsd_npz_annotations.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：310
- 函数 `parse_args`（行 23）：解析命令行参数。
- 函数 `load_items`（行 43）：从文件或配置中加载数据。
- 函数 `normalize_frame`（行 50）：把原始图像帧归一化为可视化/推理格式。
- 函数 `normalize_mask`（行 69）：把原始 mask 归一化为标准二值格式。
- 函数 `mask_bbox`（行 80）：提供该文件的一段辅助逻辑。
- 函数 `resize_to_panel`（行 87）：提供该文件的一段辅助逻辑。
- 函数 `blend_mask`（行 96）：提供该文件的一段辅助逻辑。
- 函数 `build_mask_only`（行 110）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `panel_with_title`（行 116）：提供该文件的一段辅助逻辑。
- 函数 `footer_height`（行 131）：提供该文件的一段辅助逻辑。
- 函数 `compose_sheet`（行 139）：提供该文件的一段辅助逻辑。
- 函数 `render_html`（行 188）：提供该文件的一段辅助逻辑。
- 函数 `main`（行 238）：脚本主入口，组织完整执行流程。

## `tools/visualize_refcoco_masks.py`

- 文件职责：工程工具脚本：负责训练、导出、调试、可视化或运维辅助。
- 行数：231
- 函数 `parse_args`（行 28）：解析命令行参数。
- 函数 `load_refs`（行 40）：从文件或配置中加载数据。
- 函数 `load_instances`（行 49）：从文件或配置中加载数据。
- 函数 `decode_ref_mask`（行 57）：把编码后的对象还原为可操作的数据结构。
- 函数 `truncate_text`（行 74）：提供该文件的一段辅助逻辑。
- 函数 `parse_image_id`（行 81）：提供该文件的一段辅助逻辑。
- 函数 `blend_masks`（行 85）：提供该文件的一段辅助逻辑。
- 函数 `draw_boxes_and_labels`（行 98）：提供该文件的一段辅助逻辑。
- 函数 `append_legend`（行 125）：提供该文件的一段辅助逻辑。
- 函数 `build_objects_for_image`（行 167）：构建对外可用的数据结构、模型、提示词或运行对象。
- 函数 `main`（行 189）：脚本主入口，组织完整执行流程。

